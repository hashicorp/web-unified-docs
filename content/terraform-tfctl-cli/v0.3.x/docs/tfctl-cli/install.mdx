---
page_title: Install tfctl
description: |-
  Learn how to install the tfctl CLI on macOS, Linux, and Windows using Homebrew, pre-built binaries, or by building from source.
# START AUTO GENERATED METADATA, DO NOT EDIT
created_at: 2026-07-30T22:07:33.000Z
last_modified: 2026-07-30T22:07:33.000Z
# END AUTO GENERATED METADATA
---

# Install tfctl

This page describes how to install the tfctl CLI on your system. The tfctl CLI is available through Homebrew, as pre-built binaries, or you can build it from source.

## Requirements

Before installing tfctl, ensure your system meets the following requirements:

- Operating system: macOS, Linux, or Windows
- Architecture: AMD64 (x86_64) or ARM64
- Network access to download binaries

## Install with Homebrew

MacOS and Linux users can install tfctl from the HashiCorp Homebrew tap:

```shell-session
$ brew install hashicorp/tap/tfctl
```

## Install pre-built binary

You can download official release binaries from [releases.hashicorp.com](https://releases.hashicorp.com/tfctl/).

1. Download the appropriate binary for your operating system and architecture.

1. Extract the downloaded archive and move the `tfctl` binary to a directory in your system's PATH. For example, on macOS or Linux:

   ```shell-session
   $ unzip tfctl_*.zip
   $ sudo mv tfctl /usr/local/bin/
   ```

1. Verify the installation by checking the tfctl version:

   ```shell-session
   $ tfctl --version
   ```

   The command displays the installed version of tfctl.

## Build from source

To build tfctl from source, you need Go 1.26.4 or later installed on your system.

1. Clone the tfctl repository:

   ```shell-session
   $ git clone https://github.com/hashicorp/tfctl-cli.git
   $ cd tfctl-cli
   ```

1. Build and install the binary using make:

   ```shell-session
   $ make go/install
   ```

1. Verify the installation:

   ```shell-session
   $ tfctl --version
   ```

## Install shell completion

We recommend installing the shell completion module for command, argument, and API path completion:

```shell-session
$ tfctl --autocomplete-install
```

You can uninstall shell completion with the following command:

```shell-session
$ tfctl --autocomplete-uninstall
```

## Install AI agent skill

The tfctl CLI ships with an agent skill that gives AI coding agents access to HCP Terraform through the `tfctl` command. You can install it using the `tfctl harness install` command. Replace `<agent>` with one of the following supported AI agents:

- `amp`
- `antigravity`
- `bob`
- `claude`
- `codex`
- `copilot`
- `opencode`
- `pi`

To install the skill, run:

```shell-session
$ tfctl harness install <agent> --global
```

This adds the skill to your user profile so that compatible agents can use `tfctl` commands on your behalf.

## Run tfctl in Docker

If you have a `TFCTL_TOKEN` environment variable set, you can invoke tfctl using Docker:

```shell-session
$ docker run -e TFCTL_TOKEN hashicorp/tfctl
```

Refer to the [configuration reference](/terraform/tfctl-cli/reference/cli#configuration) for more environment variables.

## Next steps

After installing tfctl, complete the following tasks:

- [Authenticate with HCP Terraform](/terraform/tfctl-cli/authenticate)
- [Review common workflows](/terraform/tfctl-cli/workflows)
