### Requests for sealwrapped values can lead to raft quorum failures ((#sealwrap-raft-quorum-failures))

<!-- BEGIN: Vault:=v2.0.x -->

| Change      | Status   | Vault edition | Affected version | Fixed version
| ----------- | -------- | ------------- | ---------------- | -------------
| Known issue | Open     | Enterprise    | 2.0.0            | No

<!-- END: Vault:=v2.0.x -->

<!-- BEGIN: Vault:=v1.21.x -->

| Change      | Status   | Vault edition | Affected version | Fixed version
| ----------- | -------- | ------------- | ---------------- | -------------
| Known issue | Open     | Enterprise    | 1.21.0           | No

<!-- END: Vault:=v1.21.x -->

<!-- BEGIN: Vault:=v1.20.x -->

| Change      | Status   | Vault edition | Affected version | Fixed version
| ----------- | -------- | ------------- | ---------------- | -------------
| Known issue | Open     | Enterprise    | 1.20.0           | No

<!-- END: Vault:=v1.20.x -->

<!-- BEGIN: Vault:=v1.19.x -->

| Change      | Status   | Vault edition | Affected version | Fixed version
| ----------- | -------- | ------------- | ---------------- | -------------
| Known issue | Open     | Enterprise    | 1.19.0           | No

<!-- END: Vault:=v1.19.x -->

In Vault 1.17.0 a change was made to assist in diagnosing deadlocks in the sealwrap code.
The deadlock detector adds overhead, and in extreme cases can prevent the Go scheduler
from scheduling the goroutines needed to maintain Raft quorum.  This would typically be
seen on clusters receiving a high volume of requests to sealwrapped mounts, particularly
after a restart or leadership change, since these result in Vault flushing its internal cache.
Vault 1.17.0 changed the sealwrap code to assist in diagnosing deadlocks. The
deadlock detector adds overhead and, in extreme cases, can prevent the Go
scheduler from scheduling the goroutines needed to maintain Raft quorum.

Reads of cached sealwrapped values do not require locking, so the Go scheduler
interference typically occurred on clusters receiving a high volume of requests
to sealwrapped mounts after a restart or leadership change due to Vault flushing
its internal cache.

Even without quorum loss, less extreme cases could still increase request
latency and CPU and memory usage and result in log entries of `POTENTIAL DEADLOCK`
followed by stack traces.

The log entries do not necessarily indicate a deadlock occured, just that a
goroutine had to wait over 30s to acquire a lock.

#### Recommendation

Upgrade to a fixed version that disables the deadlock detector by default.

You can safely leave the detector disabled. If support asks you to enable the
detector to help diagnose a deadlock issue, set the `detect_deadlocks` field in
your sever configuration to "sealwrap":

