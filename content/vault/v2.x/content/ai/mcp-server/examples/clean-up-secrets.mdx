---
page_title: "Example prompt: Delete and cleanup secrets"
description: >-
  Prompt an AI model connected to the Vault MCP server to perform secure cleanup
  operations on kv secrets.
# START AUTO GENERATED METADATA, DO NOT EDIT
created_at: 2026-08-25T20:17:22-07:00
last_modified: 2026-08-26T17:41:26.000Z
# END AUTO GENERATED METADATA
---

# Example prompt: Delete and cleanup secrets

@include 'alerts/beta.mdx'

Use the following example as a guide to help you write your own prompt to perform
secure cleanup operations on `kv` secrets.

<Tip title="Assumptions">

- You have a running HashiCorp Vault instance.
- You have the Vault MCP server running.
- You have a valid Vault authentication token with appropriate permissions for
  the resource you want to modify.
- You have a local MCP client configured (e.g., Claude for Desktop, VS Code).
- You have the `kv` plugin enabled.

</Tip>


## Prompt

```text
I need to clean up old secrets. Delete the database 
credentials from secret/legacy/old-app and remove 
the entire legacy mount if it's empty.
```

## Expected workflow

The MCP server performs the following operations:

1. **Delete specific secret**: Removes the targeted secret path
1. **List remaining secrets**: Checks if mount is empty
1. **Conditional cleanup**: Removes mount if no secrets remain

## Associated API request objects

1. Request object to delete the `old-app` credentials:

  ```json
  {
    "mount": "secret",
    "path": "legacy/old-app"
  }
  ```

1. Request object to list any remaining secrets:

  ```json
  {
    "mount": "secret", 
    "path": "legacy/"
  }
  ```

## Sample response

@include '../../../global/partials/alerts/mcp-prompt-disclaimer.mdx'

<CodeBlockConfig hideClipboard>

```text
Cleanup operations completed successfully:

## Deleted Resources
- ✅ **Secret Removed**: `secret/legacy/old-app`
  - All versions and metadata permanently deleted
  - Secret contained 3 keys (username, password, connection_string)

## Mount Analysis  
- **Path**: `secret/legacy/`
- **Status**: Empty (no remaining secrets)
- **Recommendation**: Mount can be safely removed if no longer needed

## Security Notes
- **Audit Trail**: Deletion logged in Vault audit logs
- **Recovery**: No recovery possible - ensure backups if needed
- **Access Policies**: Review and clean up associated Vault policies

Would you like me to remove the empty legacy path structure 
or keep it for future use?
```

</CodeBlockConfig>