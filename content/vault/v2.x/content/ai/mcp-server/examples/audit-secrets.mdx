---
page_title: "Example prompt: Audit secrets"
description: >-
  Prompt an AI model connected to the Vault MCP server to list and audit secrets
  manage secrets, PKI
  certificates, and system configurations.
# START AUTO GENERATED METADATA, DO NOT EDIT
created_at: 2026-08-26T03:17:30.000Z
last_modified: 2026-08-26T03:17:30.000Z
# END AUTO GENERATED METADATA
---

# Example prompt: List and audit secrets

@include 'alerts/beta.mdx'

Use the following example as a guide to help you write your own prompt to list
the current secret mounts in Vault, the key secrets in `kv`, and perform an
initial security audit.


<Tip title="Assumptions">

- You have a running HashiCorp Vault instance.
- You have the Vault MCP server [deployed](/vault/ai/mcp-server/deploy).
- You have a valid Vault authentication token with appropriate permissions for the resource you want to modify.
- You have a local MCP client configured (Claude for Desktop, VS Code, IBM Bob).
- You have a least one secret engine plugin enabled at `/secret`.

</Tip>


## Prompt

```text
I need to audit all secrets in my Vault. Show me what 
mounts exist and list the secrets in the main KV store.
```

## Expected workflow

The MCP server performs the following operations:

1. **List all mounts**: Discovers available secrets engines
1. **List secrets in KV**: Enumerates stored secrets
1. **Security analysis**: Provides audit recommendations

## Associated API request objects

1. Request object to list the available secrets plugins:

  ```json
  {
    "mount": "secret", 
    "path": ""
  }
  ```

## Sample response

@include '../../../global/alerts/mcp-prompt-disclaimer.mdx'

<CodeBlockConfig hideClipboard>

```text
# Vault Security Audit Report

## Available Mounts

| Mount Path | Type     | Description               |
|------------|----------|---------------------------|
| `secret/`. | kv-v2    | Key-Value secrets engine  |
| `pki_root/`| pki      | PKI Certificate Authority |
| `sys/`     | system   | Vault system backend.     |
| `identity/`| identity | Identity secrets engine   |

## Secrets Inventory (secret/ mount)

secret/
├── myapp/
│   ├── database
│   ├── api-keys  
│   └── certificates
├── shared/
│   └── service-accounts
└── infrastructure/
    ├── monitoring
    └── backup-configs

## Security Recommendations
- **Access Review**: Verify policies for each secret path
- **Rotation Schedule**: Implement regular secret rotation
- **Audit Logging**: Enable detailed audit logs for compliance
- **Least Privilege**: Ensure applications have minimal required access

**Total Secrets Found**: 7 secret paths across 3 applications
**Mount Health**: All mounts operational ✅
```

</CodeBlockConfig>