---
page_title: Run the Vault MCP server binary
description: >-
  Run the Vault MCP server binary directly to interact with IBM Vault APIs using
  AI agents.
# START AUTO GENERATED METADATA, DO NOT EDIT
created_at: 2026-08-26T03:17:39.000Z
last_modified: 2026-08-26T03:17:39.000Z
# END AUTO GENERATED METADATA
---

# Run the Vault MCP server binary

@include 'alerts/beta.mdx'

We recommend running the MCP server binary directly when you require a lightweight
deployment, have specific OS needs, need to minimize resource usage, or work in
environments with restricted container access.

<Tip title="Assumptions">

- You have a compatible operating system and architecture.

</Tip>

## Step 1: Install the binary 

1. Download an appropriate binary from the full
   [release library](https://releases.hashicorp.com/vault-mcp-server).

1. Add the following configuration to your client settings. Replace the 
   `/path/to/vault-mcp-server` placeholder with the local path to your downloaded
   binary.

  <CodeBlockConfig highlight="5">

  ```json
  {
    "mcp": {
      "servers": {
        "vault": {
          "command": "/path/to/vault-mcp-server",
          "args": ["stdio"],
          "env": {
            "VAULT_ADDR": "<<vault_address_here>>",
            "VAULT_NAMESPACE": "<<vault_namespace_here>>",
            "VAULT_TOKEN": "<<vault_token_here>>"
			    }
        }
      }
    }
  }
  ```

  </CodeBlockConfig>


## Step 2: Start the server

You can use the `vault-mcp-server` CLI and specify the transport protocol you
want to use to start the server. 

### Option 1: Start the server in `stdio` mode

Run the following command on the local instance to start the server in
`stdio` mode:

```shell-session
$ vault-mcp-server stdio [--log-file /path/to/log]
```

### Option 1: Start the server in `streamable-http` mode

Run the following command on the local instance to start the server in
`streamable-http` mode:

```shell-session
$ vault-mcp-server streamable-http \
    [--transport-port 8080]        \
    [--transport-host 127.0.0.1]   \
    [--mcp-endpoint /mcp]          \
    [--log-file /path/to/log]
```

<Tip title="Use environment variables">

Instead of setting values manually, consider using the
[supported environment variables](/vault/ai/mcp-server/reference/protocols#mcp-env).

</Tip>


## Next steps

- Try the [Write secrets to kv](/vault/ai/mcp-server/examples/write-kv-secret)
  example prompt to begin prompting your AI model about Vault operations.
- Ask your AI agent for help with specific Vault operations and workflows.
- Explore advanced configuration options for your specific deployment needs.
