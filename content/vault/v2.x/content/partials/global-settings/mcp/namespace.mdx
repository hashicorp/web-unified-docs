<a id="mcp-namespace" />

**`VAULT_NAMESPACE (string : <unset>)`**

Root namespace for the MCP server command. Setting a default namespace allow
relative mount paths.

**Example**: `export VAULT_NAMESPACE="admin"`
