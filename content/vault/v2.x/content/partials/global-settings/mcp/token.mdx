<a id="mcp-vault_token" />

**`VAULT_TOKEN (string : <required>)`**

A Vault-issued authentication token that authenticates the AI client to Vault.
See the [tokens concepts page](/vault/docs/concepts/tokens) for more information
on token types.

**Example**: `export VAULT_TOKEN="hvs.xxxxxxxxxxxxxxxxxxxxxxxx"`
