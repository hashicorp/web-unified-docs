1. Download and register the plugin in Vault's plugin catalog. Note that the plugin version must match exactly with
the target version from [releases.hashicorp.com](https://releases.hashicorp.com):

    ```shell-session
    $ vault plugin register -download -version="0.1.0+ent" secret vault-plugin-secrets-os
    ```

1. Enable the OS secrets engine:

    ```shell-session
    $ vault secrets enable -path=os vault-plugin-secrets-os
    ```

    By default, the secrets engine will mount at the name of the engine. To
    enable the secrets engine at a different path, use the `-path` argument.
