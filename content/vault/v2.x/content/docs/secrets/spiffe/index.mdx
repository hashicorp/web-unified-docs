---
layout: docs
page_title: SPIFFE secrets engine
description: >-
  The SPIFFE secrets engine mints SPIFFE JWT-SVIDs for workloads already
  authenticated to Vault.
---

# SPIFFE secrets engine

@include 'alerts/enterprise-only.mdx'

The `spiffe` secrets engine (Secure Production Identity Framework For
Everyone) mints JWT based SPIFFE Verifiable Identity Documents (SVIDs).
SVIDs come in two forms: an X.509 certificate or a JWT. The `sub` claim of the
token is a SPIFFE ID, and because the tokens are OIDC-compatible, anything that
already knows how to validate an OIDC ID token can validate them.

If you need X.509 certificate-based identity, use the [PKI
secrets engine](/vault/docs/secrets/pki).

## Use cases

- A workload that already holds a Vault token (from any auth method) needs
  a portable identity token to present to another service's SPIFFE
  authentication workflow, an OIDC-aware relying party, or any service that
  accepts JWTs. 
- You want SPIFFE-shaped identity tokens (`spiffe://` `sub` claims) without
  standing up another SPIFFE compatible tool.

## SPIFFE auth method vs. SPIFFE secrets engine

- Use the SPIFFE secrets engine (`secrets/spiffe`) when you are already
  logged into Vault (via Kubernetes, AppRole, AWS, etc.) and want Vault to
  issue you a SVID. See the SPIFFE secrets engine docs for details.
- Use SPIFFE auth (`auth/spiffe`) when you have SPIRE (or another SPIFFE
  issuer) running and want to log into Vault using a SVID.

|                | [SPIFFE auth method](/vault/docs/auth/spiffe)                       | SPIFFE secrets engine (this page)                              |
| -------------- | ------------------------------------------------------------------- | -------------------------------------------------------------- |
| Direction      | A workload presents an SVID **to** Vault to log in                  | Vault mints a JWT-SVID **for** a caller already logged in      |
| What it needs  | A trust bundle to validate incoming SVIDs against                   | Nothing external — it's its own signer                         |
| What it returns| A Vault token                                                       | A JWT-SVID                                                     |

## Architecture

Vault is the issuer of the SVID, not the consumer. If you need to authenticate
to Vault using SVIDs, refer to the [SPIFFE auth method](/vault/docs/auth/spiffe).

The SPIFFE secrets engine is a standalone JWT signer. It generates and rotates
its own signing key and has no dependency on the PKI secrets engine, an external 
CA, or other SPIFFE compatible service.

Each mount has a single trust domain. Roles define a claims template using
the same templating language as the
[Identity secrets engine's](/vault/docs/secrets/identity) OIDC tokens. The
fields available to interpolate depend on the auth method the workload uses to
authenticate with Vault, for example Kubernetes auth metadata,  or AppRole.
Vault always populates `iss`, `aud`, `iat`, `exp`, and a `vault.entity.id`
claim; templates can't override these.

```
  [ Workload ]                                        [ Vault ]
  (already authenticated                              (mints and signs
   by any other method)                                 the JWT-SVID)
       │                                                    │
       │──(1. Request JWT-SVID, mintjwt)──────────────────> │
       │ <──────────────────(2. JWT-SVID)────────────────── │
       │
       │──(3. Present JWT-SVID)──────────────────> [ Relying party ]
                                                     (validates via JWKS,
                                                      accepts)
```

## Supported SVID types

Vault's SPIFFE secrets engine mints JWT based SVIDs:

- **JWT-SVIDs**: The client fetches a short-lived JWT from its local SPIFFE
  Workload API and sends it as a bearer token. Vault validates the signature
  against the trust bundle's JWKS and checks the `aud` claim against the
  mount's audience allow-list.

Vault mints X.509-SVIDs using the PKI secrets engine.

## Next steps

- [Enable and set up](/vault/docs/secrets/spiffe/spiffe) the SPIFFE secrets
  engine, configure the trust domain, and define roles.
- [Mint SVIDs with Vault](/vault/docs/secrets/spiffe/mint-svid) and verify a
  JWT-SVID end to end.
