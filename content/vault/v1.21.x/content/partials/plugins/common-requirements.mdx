- **Permissions**: You must have admin permissions for Vault to run `plugin register` and the appropriate `enable` command.
- **Vault Configuration**: You must have [`plugin_directory`](/vault/docs/configuration#plugin_directory) and [`api_addr`](/vault/docs/configuration#api_addr) set in your configuration file.
