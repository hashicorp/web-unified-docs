# Choose your workflow

Describes how to establish team workflows, remote backend, state locking, using HCP TF/TFE, local vs remote, links to configure TF block

##  Local execution 

Separate page at `/terraform/docs/workflow/local` ?

- Local vs remote state storage (using backend blocks), 
- Configure remote state storage
   - State storage only, not execution environment

## Remote execution 

Separate page at `/terraform/docs/workflow/remote` ?

- What is remote execution vs just remote state storage? Link to corresponding run pages in HCP TF/TFE docs? 
- Start to introduce HCP TF benefits, CLI-, VCS-, API-driven workflows 

## Automate Terraform workflows 
Separate page at `/terraform/docs/workflow/automate` ?

- Using terraform plan to generate a plan file, review, cred and variable management for automation?, intro to HCP TF taking care of all of this, link to that section 
- Integrate with CI/CD pipelines (/ci-cd)
- Use GitHub Actions (/github-actions)
