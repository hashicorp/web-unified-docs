# Overview

> What is the infrastructure creation workflow? State/config/infra relationship. Create config + apply changes. State tracking. How to make config dynamic (expressions, meta-args). What is the dependency graph?