---
page_title: Authenticate with HCP Terraform
description: |-
  Learn how to authenticate tfctl with HCP Terraform or Terraform Enterprise using login credentials. Configure authentication tokens for API access.
# START AUTO GENERATED METADATA, DO NOT EDIT
created_at: 2026-07-30T22:07:32.000Z
last_modified: 2026-07-30T22:07:32.000Z
# END AUTO GENERATED METADATA
---

# Authenticate with HCP Terraform

This page describes how to authenticate tfctl with HCP Terraform or Terraform Enterprise. Authentication is required before you can use tfctl to manage runs, variables, and other resources.

## Requirements

Before authenticating, ensure you have:

- tfctl installed on your system
- An HCP Terraform or Terraform Enterprise account
- Network access to your HCP Terraform or Terraform Enterprise instance

## Set hostname

The default hostname is the HCP Terraform instance at `app.terraform.io`. To use a different HCP Terraform instance or your organization's Terraform Enterprise instance, use the [`tfctl profile set`](/terraform/tfctl-cli/reference/cli/profile/set) command:

```shell-session
$ tfctl profile set hostname <host>
```

Replace `<host>` with your Terraform Enterprise hostname.

## Authenticate with login

Complete the following steps to authenticate tfctl:

1. Run the login command:

   ```shell-session
   $ tfctl auth login
   ```

   The command opens your default web browser and prompts you to authorize tfctl.

1. In your browser, click the **Create an API token** button, then give your token a description and set its expiration.

1. Click **Generate token**, then copy the new token.

1. Return to your terminal and paste the token. The CLI does not print the pasted token to the screen.

1. Verify that the login is successful before leaving the token page in your browser, because HCP Terraform does not show the token once it's closed.

1. Verify your authentication status:

   ```shell-session
   $ tfctl auth status
   ```

   The command displays your authentication status and the associated user.

## Authentication token storage

tfctl stores authentication tokens in the following directories, depending on your operating system:

- **Linux/MacOS**: `~/.config/tfctl`
- **Windows**: `%AppData%/tfctl`

The CLI stores configuration for individual profiles in the `profiles` subdirectory. As with Terraform, tfctl stores these tokens in plain text.

## Terraform tokens

If you have not configured a token for the active profile with `tfctl auth login`, the tfctl CLI checks your Terraform configuration for a matching token. These tokens may either be in your Terraform configuration directory (for example, `~/.terraform.d/credentials.tfrc.json`) or the corresponding Terraform environment variables (such as `TF_TOKEN_app_terraform_io`).

## Next steps

After authenticating, you can:

- [Set your default organization](/terraform/tfctl-cli/workflows#configure-profile-settings)
- [Configure your profile settings](/terraform/tfctl-cli/reference/cli/profile)
- [Review common workflows](/terraform/tfctl-cli/workflows)
- [Start managing runs](/terraform/tfctl-cli/reference/cli/run)
