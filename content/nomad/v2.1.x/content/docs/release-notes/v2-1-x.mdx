---
layout: docs
page_title: Nomad v2.1.x release notes
description: >-
  HashiCorp Nomad version 2.1.x release notes. Release highlights include client node identity, client node introduction, job specification secret block, and system job deployment support.
---

# Nomad 2.1.x release notes

@include '../../../global/partials/version-eol-chart.mdx'

## 2.1.0 release highlights

Release Date: 2026

### New nomad operator snapshot command

The `nomad operator snapshot filter` command  removes selected entries from an
existing snapshot file. Refer to the [`nomad operator snapshot filter` command
reference](/commands/operator/snapshot/filter) for details.

### Server join requires authentication

Using the `server join` command or the [Join
Agent](/nomad/api-docs/agent#join-agent) API requires a token with the
`agent:write` permission.


### Removed deprecated server configuration (breaking change)

The deprecated server configuration parameters `server.retry_join`,
`server.retry_interval`, `server.retry_max`, and `server.start_join` have been
removed in Nomad 2.1.0. Use the
[`server.server_join`](/nomad/docs/configuration/server#server_join)
configuration block instead.

### Changelog

Review improvements, security fixes, and breaking changes in the changelog.

- [v2.1.0](https://github.com/hashicorp/nomad/releases/tag/v2.1.0)