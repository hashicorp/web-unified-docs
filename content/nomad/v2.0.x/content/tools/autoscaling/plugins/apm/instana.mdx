---
layout: docs
page_title: Nomad Autoscaler Instana APM plugin
description: |
  Use the Nomad Autoscaler Instana APM plugin to query infrastructure metrics from Instana.
# START AUTO GENERATED METADATA, DO NOT EDIT
created_at: 2026-06-19T16:24:15.000Z
last_modified: 2026-06-19T16:24:15.000Z
# END AUTO GENERATED METADATA
---

# Instana APM plugin

Use the `instana` APM plugin to query infrastructure metrics from
[Instana][instana_homepage] for scaling decisions.

## Agent configuration options

```hcl
apm "instana" {
  driver = "instana"

  config = {
    endpoint  = "https://example.instana.io"
    api_token = "<api token>"
  }
}
```

- `endpoint` `(string: <required>)` - The absolute URL of the Instana backend,
  including the scheme. For example, use `https://example.instana.io` for a
  SaaS tenant or the base URL for a self-hosted Instana backend.

- `api_token` `(string: <required>)` - The Instana API token to use for
  authentication. You can also set the `INSTANA_API_TOKEN` environment variable
  instead of configuring this value in the agent configuration. When both are
  set, `api_token` takes precedence.

## Policy configuration options

```hcl
check {
  source       = "instana"
  query_window = "5m"
  query = jsonencode({
    plugin  = "host"
    query   = "entity.zone:production"
    metrics = ["cpu.used"]
    rollup  = 60
  })
  # ...
}
```

The `query` value must be the Instana infrastructure metrics API request body,
encoded as JSON in HCL. Use `jsonencode({ ... })` to encode the request body.
Do not include `timeFrame` in the query JSON. The autoscaler derives the
`timeFrame` value from the check's `query_window` and sends it with the request
to Instana.

The query JSON supports the following fields:

- `plugin` `(string: <required>)` - The Instana infrastructure plugin to query,
  such as `host`.

- `metrics` `(list<string>: <required>)` - The Instana metric IDs to query, such
  as `cpu.used`.

- `query` `(string: "")` - An Instana infrastructure query expression for
  filtering the entities that return metrics.

- `snapshotIds` `(list<string>: [])` - A list of Instana snapshot IDs to query
  directly.

- `rollup` `(number: 0)` - The rollup interval for the Instana metrics request.

<Note>

The Instana plugin does not support an instant `query_window`. Use a
duration-based window instead.

</Note>

The plugin returns one time series for each entity snapshot and metric ID pair in
the Instana response. For scaling policies that expect a single metric stream,
configure the Instana query so it returns one snapshot and one metric ID.

When Instana rate limits a metrics request, the autoscaler reports the rate-limit
reset value from Instana. When a query returns no data points, increase the
`query_window` or narrow the Instana query to entities that report the requested
metric.

[instana_homepage]: https://www.ibm.com/products/instana
