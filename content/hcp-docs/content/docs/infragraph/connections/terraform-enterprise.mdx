---
page_title: Connect Infragraph to Terraform Enterprise
description: |-
  The Terraform Enterprise connection imports your Terraform Enterprise data into the resource graph. Learn how to set up the connection, sync data, and view Terraform Enterprise data in Infragraph.
---

# Connect Infragraph to Terraform Enterprise

The Terraform Enterprise connection connects your Terraform Enterprise data to the resource graph.

@include 'beta-feature.mdx'

## Create a Terraform Enterprise team

We recommend you create a new Terraform Enterprise team to use for your Infragraph connection. To give Infragraph access to all workspaces in your Terraform Enterprise instance, enable the following permissions:

- [**View all projects**](/terraform/enterprise/users-teams-organizations/permissions/organization#view-all-projects)
- [**View all workspaces**](/terraform/enterprise/users-teams-organizations/permissions/organization#view-all-workspaces)

You can use Terraform Enterprise permissions to limit the scope of your API token to specific projects or workspaces. Refer to the [Manage teams](/terraform/enterprise/users-teams-organizations/teams/manage#manage-workspace-access) documentation for more information.

## Create an Terraform Enterprise team token

To connect Infragraph to Terraform Enterprise, you must provide an API token. Refer to the [Team API Tokens](/terraform/enterprise/users-teams-organizations/api-tokens#team-api-tokens) documentation for more information.

## Set up the connection 

Complete the following steps to establish a new Terraform Enterprise connection:

1. From the Resource graph **Overview** at the top right, click **Create Connection**.
1. Select **Terraform Enterprise** from the list of data sources. 
1. Click **Authenticate Connection**. 
1. Under **TFE Hostname**, enter the address of your Terraform Enterprise instance. 
1. Follow the instructions to paste the team token.
1. Click the **Review connection details** button.
1. Review the connection information. 
1. Click the **Create connection** button.

The data source connects to your Terraform Enterprise organization. 

## Sync Terraform Enterprise data 

@include 'infragraph/sync-data.mdx'

## View Terraform Enterprise data  

To find Terraform Enterprise data connected to your resource graph, complete any of the following actions: 

- Go to the **Inventory** page. Click an individual resource type to view it in the graph explorer. 
- Query resources with a saved query that includes Terraform Enterprise resources.
- Build a custom query using Terraform Enterprise resources in the query editor. For easy filtering, you can select **IaC** or **Management** options.
