---
page_title: hcp waypoint actions read
description: |-
  The "hcp waypoint actions read" command lets you read more details about an action.
---

# hcp waypoint actions read

Command: `hcp waypoint actions read` 

The `hcp waypoint actions read` command returns more details about an action
configurations.

## Usage

```shell-session
$ hcp waypoint actions read --name [Optional Flags]
```

## Required flags

- `-n, --name` - The name of the action.

