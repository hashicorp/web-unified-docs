---
page_title: apps create - Command
description: |-
  The "apps create" command create a new application in the project.
---

# apps create 

Command: `vlt apps create`

The `apps create` command creates a new application in the currently configured
target project.

### Examples

Create a new application named "company-card" with a description.


```shell-session
$ vlt apps create --description="Stores corporate card info" company-card
Successfully created app with name company-card
```

<Note>

To interact with the newly created application, be sure to run the `vlt config init`
command again to set the target application.

You can run the `vlt config` to check the current configuration.

</Note>


## Usage

<CodeBlockConfig hideClipboard>

```shell-session
$ vlt apps create [options] [app_name]
```

</CodeBlockConfig>


### Command options

- `--description` `(string: "")` - Description for the application to create.
  Use `-d` for its shorthand.
- `--organization` `(string: "")` - HCP organization that the application is in.
  Use `-o` for its shorthand.
- `--project` `(string: "")` - HCP project that the application is in. Use `-p`
  for its shorthand.
- `--format` `(string: "")` - The output format you would like (json). Use `-f`
  for its shorthand.

