<Note title="Experimental">

The `terraform stacks migrate` command group is experimental and disabled by default.
Set `TF_STACKS_MIGRATE_EXPERIMENTAL=true` to enable these commands. Do not use these
commands on production resources.

</Note>