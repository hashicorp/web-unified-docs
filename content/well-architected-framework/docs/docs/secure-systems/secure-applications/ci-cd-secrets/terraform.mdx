---
page_title: Terraform
description: Integrate Terraform with HashiCorp Vault using OIDC authentication. Retrieve secrets in infrastructure code and use dynamic credentials for cloud providers.
---

# Terraform

Terraform Cloud and Terraform Enterprise provide native OIDC integration with Vault, allowing infrastructure deployments to authenticate to Vault and retrieve secrets without storing long-lived credentials. When you use OIDC authentication, Terraform automatically generates short-lived identity tokens for each run that Vault validates to grant access to secrets and dynamic credentials.

## Why integrate Terraform with Vault

Integrating Terraform with Vault addresses the following security and operational challenges in infrastructure automation:

**Eliminate credentials from Terraform configurations:** Vault keeps sensitive values out of Terraform code, state files, and version control by providing secrets on-demand during runs. Hard-coded credentials in `.tf` files or workspace variables create security risks because they persist in state files and can be exposed through misconfigured storage or logs.

**Enable dynamic cloud credentials:** Vault generates temporary AWS, Azure, and GCP credentials for each Terraform run that automatically expire, eliminating the need to store long-lived cloud provider credentials in Terraform Cloud workspaces. Dynamic credentials reduce the attack surface by providing time-limited access that expires when the run completes.

**Support infrastructure secret requirements:** Infrastructure deployments require various secrets including database credentials, API keys for external services, TLS certificates, and encryption keys. Vault centralizes management of these secrets with unified access control, rotation policies, and audit logging across all infrastructure deployments.

**Meet compliance and audit requirements:** Organizations need detailed audit trails showing which Terraform workspaces accessed which secrets during which runs, but Terraform state files and logs alone do not provide the access controls and granularity required for regulatory compliance. Vault provides comprehensive audit logs that track every secret access with workspace identity and timestamp, supporting compliance frameworks like SOC 2 and PCI DSS.

## Choose an integration approach

Terraform supports several approaches for integrating with Vault. Choose from the following approaches based on your deployment model and security requirements:

| Approach | Setup Complexity | Credentials Required | Secret Access | Best For |
|----------|-----------------|---------------------|---------------|----------|
| [Terraform Cloud OIDC](/terraform/cloud-docs/dynamic-provider-credentials/vault-configuration) | Low | None (OIDC tokens) | Vault provider in Terraform code | Teams using Terraform Cloud or Enterprise |
| [Vault-backed dynamic credentials](/terraform/cloud-docs/dynamic-provider-credentials/vault-backed) | Medium | None (OIDC tokens) | Automatic cloud provider credentials | Teams wanting to eliminate cloud credentials |
| [Terraform in CI/CD](/well-architected-framework/secure-systems/secure-applications/ci-cd-secrets) | Medium | AppRole or OIDC | Environment variables before Terraform runs | Teams running Terraform in GitHub Actions, CircleCI, etc. |

- **Terraform Cloud OIDC integration:** Terraform Cloud and Terraform Enterprise provide native OIDC integration with Vault that eliminates the need to store Vault credentials. Terraform automatically authenticates to Vault during each run using short-lived OIDC tokens, retrieves secrets using the Vault provider in your Terraform code, and revokes tokens when runs complete. Use this approach when running Terraform Cloud or Terraform Enterprise.

- **Vault-backed dynamic credentials:** Terraform Cloud can use Vault to generate temporary cloud provider credentials (AWS, Azure, GCP) for each run instead of storing long-lived credentials in workspace variables. Terraform authenticates to Vault using OIDC, Vault generates dynamic credentials from its secrets engines, and Terraform uses those credentials to provision infrastructure. Use this approach to eliminate cloud provider credentials from Terraform Cloud entirely.

- **Terraform in CI/CD pipelines:** When running Terraform CLI in CI/CD platforms like GitHub Actions or CircleCI, the CI/CD platform authenticates to Vault using AppRole or OIDC, retrieves secrets and sets them as environment variables, then runs Terraform commands. Use this approach when using Terraform CLI in existing CI/CD pipelines rather than Terraform Cloud. Refer to the platform-specific integration guides for detailed configuration.

## Best practices

The following best practices help you secure and operationalize Vault integration with Terraform:

- **Configure JWT roles with workspace bound claims:** When using OIDC authentication, configure Vault JWT auth method roles with `bound_claims` that restrict authentication to specific Terraform organizations, workspaces, and run phases. Use claims like `sub` with the format `organization:my-org:workspace:production:run_phase:*` to ensure only authorized workspaces can authenticate.

- **Set token TTLs matching run duration:** Configure Vault token TTL values that match your typical Terraform run duration, typically 15-30 minutes for most plans and applies. HCP Terraform automatically handles token renewal for longer-running operations, but short TTLs reduce the risk window.

- **Enforce least-privilege policies per workspace:** Create Vault policies that grant each Terraform workspace the minimum permissions required for its infrastructure deployments. Create separate policies for different environments (development, staging, production) with each policy restricted to specific secret paths.

- **Use Vault-backed dynamic credentials for cloud providers:** Prefer Vault-backed dynamic credentials over storing long-lived AWS, Azure, or GCP credentials in HCP Terraform workspace variables. Vault generates fresh credentials for each run and automatically revokes them when runs complete, eliminating manual credential rotation.

## HashiCorp resources

Terraform Cloud and Vault integration:

- Read the [dynamic credentials with Vault](/terraform/cloud-docs/dynamic-provider-credentials/vault-configuration) documentation for complete OIDC configuration
- Learn about [Vault-backed dynamic credentials](/terraform/cloud-docs/dynamic-provider-credentials/vault-backed) for generating cloud provider credentials through Vault
- Explore [dynamic credentials setup examples](https://github.com/hashicorp/terraform-dynamic-credentials-setup-examples/tree/main/vault) for working Vault configurations
- Read the [Vault provider documentation](https://registry.terraform.io/providers/hashicorp/vault/latest/docs) for Terraform data sources and resources
- Learn about [managing sensitive variables](/terraform/tutorials/configuration-language/sensitive-variables) in Terraform configurations

Vault authentication and secrets:

- Learn about [JWT/OIDC auth method](/vault/docs/auth/jwt) for token-based authentication and bound claims configuration
- Learn about [dynamic and static secrets](/vault/tutorials/get-started/understand-static-dynamic-secrets) to choose the right approach for your infrastructure
- Read the [KV secrets engine documentation](/vault/docs/secrets/kv) for static secret storage
- Read the [Vault policies documentation](/vault/docs/concepts/policies) for ACL policy syntax and least-privilege patterns

Vault secrets engines for cloud providers:

- Learn about [AWS secrets engine](/vault/docs/secrets/aws) for dynamic AWS credentials
- Learn about [Azure secrets engine](/vault/docs/secrets/azure) for dynamic Azure service principals
- Learn about [GCP secrets engine](/vault/docs/secrets/gcp) for dynamic GCP service account keys
- Learn about [database secrets engine](/vault/docs/secrets/databases) for dynamic database credentials

Terraform and Vault best practices:

<VideoEmbed url="https://www.hashicorp.com/en/resources/best-practices-for-using-terraform-with-vault"/>

## Next steps

In this section of [Managing CI/CD secrets](/well-architected-framework/secure-systems/secure-applications/ci-cd-secrets), you learned about Terraform and Vault integration. Terraform and Vault integration is part of the [Secure systems pillar](/well-architected-framework/secure-systems).

- Review the [CI/CD secrets overview](/well-architected-framework/secure-systems/secure-applications/ci-cd-secrets) for architectural guidance across platforms
- Compare [dynamic and static secrets in CI/CD](/well-architected-framework/secure-systems/secure-applications/ci-cd-secrets/dynamic-and-static-secrets) to choose the right secrets strategy
