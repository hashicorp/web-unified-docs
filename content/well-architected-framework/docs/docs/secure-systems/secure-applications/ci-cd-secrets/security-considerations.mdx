---
page_title: CI/CD security considerations
description: Apply common security best practices when designing your CI/CD secrets management strategy.
---

# CI/CD Security considerations

There are several security considerations you need to align with your
organizational risk tolerance and operational constraints when designing your
CI/CD secrets management strategy. The following are some best practices to
consider:

- **Namespace and service account isolation:** Every distinct pipeline workload
  should have its own service account with its own Vault role. A shared service
  account across pipelines is a lateral movement path — if one pipeline is
  compromised, it can access secrets scoped to any other pipeline sharing that
  identity. 
- **Policy least privilege:** Vault policies for CI/CD roles should grant read
  access only to the specific paths that pipeline needs. Wildcard paths
  (secret/ci/*) are convenient but should be avoided for production pipelines —
  the blast radius of a compromised pipeline token is the entire subtree. 
- **Token TTL alignment:** Pipeline token TTLs should be set to slightly exceed
  the expected maximum pipeline duration, not to a round number like 24h. A
  token that outlives the pipeline it was issued for is a credential left on the
  table. 
- **Log masking:** When using Vault Agent sidecar patterns with Vault Agent
  templates, secrets written to files are not automatically masked in Tekton
  step logs. Any step that reads a secret from a file and logs it — directly or
  via a misconfigured tool — will expose it. Environment variables can also be
  exposed through subprocesses, debug output, or crash data. Prefer short-lived
  files or explicit step inputs with careful command design, and treat Vault
  audit logs as the source of truth for secret access rather than pipeline logs.
- **JWT role constraints:** For JWT auth, always configure the most specific
  role constraints possible. `bound_claims` is the most flexible option when
  claims carry workload identity, but `bound_audiences` and `bound_subject` also
  narrow authorization. An unconstrained JWT role that accepts any token from
  the issuer allows any workload with access to that OIDC provider to
  authenticate as a Vault client. 
- **Namespace isolation for multi-team environments:** In organizations where
  multiple teams share a Tekton cluster, Vault namespaces in Vault Enterprise
  or HCP Vault Dedicated provide hard isolation boundaries for secrets,
  policies, and auth methods per team. This prevents a team from accidentally
  or intentionally accessing another team's Vault configuration.

  ## Next steps

  Refer to the product specific documentation for your CI/CD platform to see how
  to plan and design your secrets management strategy in your environment.
  