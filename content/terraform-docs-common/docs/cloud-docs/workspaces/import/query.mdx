---
page_title: Run queries and import resources
description: Learn how to run queries to discover unmanaged infrastructure and import resources into your workspace. Use filters and bulk operations to manage infrastructure at scale.
---

# Run queries and import resources

This page describes how to run queries to discover and import resources into your workspace. You can search across cloud providers, filter results, and import resources individually or in bulk.

To learn more about the search and import feature, refer to the [Search and import overview](/terraform/cloud-docs/workspaces/import).

## Background

HCP Terraform executes search queries against your cloud providers to discover resources that are not currently managed by Terraform. 

The search interface displays results with management status indicators, helping you identify which resources to import. You can generate Terraform configuration for selected resources and apply it to bring them under management.

## Requirements

To run queries and import resources, you need:

- An HCP Terraform workspace with Terraform 1.14.0 or newer configured
- Cloud provider credentials for the providers you wish to query configured for your workspace
- Read and write permissions for the workspace

FIXME: Requirements correct?

### HCP Terraform agents

If you use HCP Terraform agents for your runs, you must enable the `query` operation when starting the agent pool. Refer to [Install and run agents](/terraform/cloud-docs/agents/agents) for more information.

FIXME: Agent Requirements correct?

### Permissions

Your HCP Terraform user must have the following permissions to run queries and import resources:

- **Manage workspace** permission for the workspace

FIXME: Permissions correct?

## Define queries

To query resources for your workspace:

1. Navigate to your workspace in HCP Terraform.
1. Ensure that your workspace and user meet the [requirements](#requirements) to query infrastructure.
1. Select **Search & Import** from the left navigation.
1. Click the **+ New query** dropdown in the upper right.
1. Select the **Build query via UI** button.
1. On the **New query** page, select the providers you wish to import resources for. Your workspace must have credentials configured for the providers you select.
1. Click the **Select provider** button to continue.
1. To begin building your query, select the resource types you want to query for. You can include multiple resource types from different providers in a single query.
1. To filter your query to only match certain resources of type you selected, click the **+** icon to the right of your resource type.
1. You can select pre-defined filters based upon the resource type, or filter any attribute that Terraform tracks for the resource type.

## Run queries

After you have defined all of the filters for your query, click the **Run query** button to run your query. A **Start new query** modal will pop up. To have HCP Terraform generate starter configuration for you, leave the **Generate starter configuration** box checked. Click **Confirm** to have HCP Terraform query your resources.

## Review results

HCP Terraform displays the query results, including the IaC status, if known.

The **IaC Status** colum will report whether the resource matches one already being managed by HCP Terraform:
- **Managed**: The resource has the same [identity](/terraform/language/import#resource-identity) as a resource managed by a workspace with a similar provider version.
- **Unknown**: The resource has the same [identity](/terraform/language/import#resource-identity) as a resource managed by an older Terraform or provider version. Verify the resource before importing.
- **Unmanaged**: The resource is not currently managed by Terraform and can be imported.

HCP Terraform identifies resources managed by other workspaces when those workspaces use Terraform v1.12 or newer. When a provider changes its [resource identity](/terraform/language/import#resource-identity) definition between versions, HCP Terraform may list a resource as **Unknown** instead of **Unmanaged**.

### Filter and search results

Use the search bar and filters to narrow results:

- Enter text in the search bar to filter by resource name or identifier.
- Use the **Resource type** filter to show only specific resource types.
- Use the **IaC status** filter to show only managed, unmanaged, or unknown resources.
- Click on a resource in the **Resource type** column to view detailed attributes provided by the provider.

## Generate configuration

Select one or more resource instances in the search results area and click **Generate Starter Configuration**. HCP Terraform generates `import` and `resource` blocks that you can add to your configuration to import the resource instances into your state.

## Import resources
Copy the code to your Terraform configuration and apply the configuration. Before applying the configuration, we recommend running the [`terraform fmt` command](/terraform/cli/commands/fmt) to ensure that the code is properly formatted.

Use one of the following methods to apply the configuration:
  
- Navigate to your workspace in HCP Terraform and click **New run**.
- Run the `terraform apply` command on your workstation.
- If your workspace is configured to run on VCS changes, check the updated configuration into your VCS to trigger a new run. 

## Next steps

After importing the resources to state, you can delete the generated import blocks or keep them as a historical record.  
