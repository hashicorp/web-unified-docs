---
page_title: Configure SCIM provisioning
description: >-
  Learn how to configure SCIM provisioning and set up supported identity
  providers such as Okta and Microsoft Entra ID.
# START AUTO GENERATED METADATA, DO NOT EDIT
created_at: 2026-08-05T15:05:42-07:00
last_modified: 2026-08-26T23:37:47.000Z
# END AUTO GENERATED METADATA
---

<!-- BEGIN: TFEnterprise:only -->
# Configure SCIM provisioning in Terraform Enterprise
<!-- END: TFEnterprise:only -->
<!-- BEGIN: TFC:only -->
# Configure SCIM provisioning in HCP Terraform
<!-- END: TFC:only -->

This topic describes how to enable SCIM provisioning in <!-- BEGIN: TFEnterprise:only name:product -->Terraform Enterprise<!-- END: TFEnterprise:only name:product --><!-- BEGIN: TFC:only name:product -->HCP Terraform<!-- END: TFC:only name:product --> and generate the credentials required to connect your identity provider. After completing these steps, configure your identity provider to complete the setup. 

<!-- BEGIN: TFC:only name:pnp-callout -->

@include 'tfc-package-callouts/paid-plan.mdx'

<!-- END: TFC:only name:pnp-callout -->

<!-- BEGIN: TFEnterprise:only name:scim-states -->
## Overview

The following states control how Terraform Enterprise processes SCIM provisioning requests.

### SCIM states

| State | What it means |
|-------|---------------|
| **Disabled** | Default state. Terraform Enterprise does not process public SCIM provisioning requests to `/scim/v2/Users` or `/scim/v2/Groups`. You cannot pause SCIM until it is enabled. |
| **Enabled** | Terraform Enterprise processes public SCIM provisioning requests to `/scim/v2/Users` and `/scim/v2/Groups`. Your identity provider can create and update users and groups. Team membership sync happens only for teams that are linked to a SCIM group. |
| **Paused** | SCIM stays enabled, but Terraform Enterprise stops processing public SCIM provisioning requests to `/scim/v2/Users` and `/scim/v2/Groups`. Existing SCIM users, groups, team links, and tokens stay in place. Users can still sign in with SAML. |

When SCIM is enabled and not paused, Terraform Enterprise can create and update users and groups from incoming SCIM requests. Team membership does not change until you link a SCIM group to a Terraform Enterprise team.
<!-- END: TFEnterprise:only name:scim-states -->

## Requirements

You must meet the following requirements to enable SCIM<!-- BEGIN: TFEnterprise:only name:requirements -->:<!-- END: TFEnterprise:only name:requirements --><!-- BEGIN: TFC:only name:requirements -->.<!-- END: TFC:only name:requirements -->

<!-- BEGIN: TFEnterprise:only name:requirements -->
- You must be a site administrator. For more information, refer to [Site Administration Permissions](/terraform/enterprise/users-teams-organizations/users#site-admin-permissions).
- SAML SSO must already be enabled. SCIM handles provisioning. SAML handles sign-in. For setup steps, refer to [Configure SAML](/terraform/enterprise/saml/configuration).
- Configure SCIM on the same identity provider that you use for SAML SSO.
- Terraform Enterprise supports SCIM with Microsoft Entra ID, Okta, and supported generic SAML provider configurations. HashiCorp provides setup guides for Okta and Microsoft Entra ID.
- Keep at least one [non-SSO admin account for recovery](/terraform/enterprise/saml/troubleshooting#create-a-non-sso-admin-account-for-recovery).
<!-- END: TFEnterprise:only name:requirements -->

<!-- BEGIN: TFC:only name:requirements -->
### Account

- Your HCP Terraform organization must be on an Essentials, Standard, or Premium plan.
- You must be a member of the [owners team](/terraform/cloud-docs/users-teams-organizations/permissions/organization#organization-owners) for your HCP Terraform organization.
- Your organization must be in a standalone HCP Terraform account. Organizations in accounts integrated with HCP are not supported.

### Identity provider

- You must have already configured SAML SSO for your identity provider in your organization. You cannot use the HashiCorp Cloud Platform (HCP) SSO integration for SCIM.
- You must use either Okta or Microsoft Entra ID as your identity provider. Okta requires a custom application because the Okta integration network gallery application for HCP Terraform is incompatible with SCIM for HCP Terraform.

### Recommendations

We recommend creating a non-production organization for testing before you enable SCIM in production. 

You should also maintain at least two manually managed accounts in the owners team for emergency access. You cannot manage the owners team with SCIM, and users managed through SCIM cannot serve as emergency break-glass accounts because they lack password authentication.

Review your existing IdP group structure and team memberships before enabling SCIM. When SCIM synchronizes a group that already exists in HCP Terraform, the IdP group takes precedence and replaces existing membership.
<!-- END: TFC:only name:requirements -->

## Enable SCIM
<!-- BEGIN: TFEnterprise:only name:enable-scim -->

1. Open your user icon menu and click **Site Admin**, or go to `https://<TFE_HOSTNAME>/app/admin/scim`.
1. Click **Enable SCIM provisioning**.
1. Copy the SCIM base URL. Use `https://<TFE_HOSTNAME>/scim/v2` when you configure your identity provider.

After you enable SCIM, generate a token and configure your identity provider.

For API-based configuration, refer to the [Admin SCIM Settings API](/terraform/enterprise/api-docs/admin/scim-settings).
<!-- END: TFEnterprise:only name:enable-scim -->

<!-- BEGIN: TFC:only name:enable-scim -->

1. Log in to HCP Terraform and navigate to your organization.
1. Click **Organization settings** and then **SCIM provisioning**.
1. Click **Enable SCIM**.
1. Click **Generate SCIM token**.
1. Set an expiration date between 30 days and one year from the creation date. SCIM tokens in HCP Terraform have mandatory expiration dates. Rotate the token before it expires to avoid interruptions to SCIM synchronization.
1. Copy and store the token securely before closing the dialog. The token is displayed only once. If you close the the dialog before saving the token, you must generate a new token. You need the token to configure your identity provider.
1. Copy and save the URL from the **Base URL** field.

<!-- END: TFC:only name:enable-scim -->

<!-- BEGIN: TFEnterprise:only name:create-token -->
## Create a SCIM token

Next, create a SCIM token for your identity provider so that it can authenticate with Terraform Enterprise:

1. On the SCIM settings page in Terraform Enterprise, navigate to the **SCIM Tokens** section.
1. Click **Create Token**.
1. Enter a **Description** for the token, such as `Microsoft Entra ID Provisioning`.
1. It is optional, but we recommend configuring an token expiration.
1. Click **Create Token**.
1. Copy the token value and store it securely. You need this token when configuring Entra ID.

Terraform Enterprise only displays the token value once. You cannot retrieve the token value after you close the dialog. If you lose the token, you must create a new one.

You can keep more than one active SCIM token at the same time. This lets you rotate tokens without downtime. For detailed token management steps, refer to [Tokens](/terraform/cloud-docs/scim/manage/tokens).
<!-- END: TFEnterprise:only name:create-token -->


## Configure your identity provider

Follow the instructions for your IdP:

- [Okta](/terraform/cloud-docs/scim/configure/okta)
- [Microsoft Entra ID](/terraform/cloud-docs/scim/configure/entra-id)

<!-- BEGIN: TFEnterprise:only name:idp-handoff -->
You must have the following information:

- The SCIM base URL: `https://<TFE_HOSTNAME>/scim/v2`
- A SCIM token generated in Terraform Enterprise
- The attribute mappings required by your identity provider
<!-- END: TFEnterprise:only name:idp-handoff -->

<!-- BEGIN: TFC:only name:idp-handoff -->
You must have the following information generated during the [Enable SCIM](#enable-scim) step:

- The SCIM base URL from **Organization settings** > **SCIM provisioning**
- The SCIM token generated when you enabled SCIM

You must also create a custom SAML application in your identity provider before configuring SCIM. The existing gallery application for Terraform Cloud does not support SCIM for HCP Terraform. Refer to your identity provider setup guide for instructions.
<!-- END: TFC:only name:idp-handoff -->

## Next steps

Follow the instructions for your IdP to complete the setup:

- [Okta](/terraform/cloud-docs/scim/configure/okta)
- [Microsoft Entra ID](/terraform/cloud-docs/scim/configure/entra-id)

<!-- BEGIN: TFEnterprise:only name:next-steps -->
After initial setup, use the following topics to manage and troubleshoot SCIM provisioning:

- Maintain the site-level SCIM configuration, including pause and resume behavior, site admin group mapping, token rotation, and disabling SCIM. Refer to [Manage SCIM](/terraform/cloud-docs/scim/manage).
- Manage group-to-team links and team-level synchronization behavior. Refer to [Link SCIM groups to teams](/terraform/cloud-docs/scim/manage/team-mapping).
- Troubleshoot provisioning, synchronization, and token issues. Refer to [Troubleshoot SCIM provisioning](/terraform/cloud-docs/scim/troubleshooting).

Refer to [Integration settings](/terraform/enterprise/application-administration/integration) for the admin interface reference for the **Enable SCIM provisioning** setting.

### API reference

- [Admin SCIM Settings API](/terraform/enterprise/api-docs/admin/scim-settings)
- [Admin SCIM Tokens API](/terraform/enterprise/api-docs/admin/scim-tokens)
- [Team SCIM Group Mapping API](/terraform/enterprise/api-docs/admin/team-scim-group-mapping)
- [Public SCIM API](/terraform/enterprise/api-docs/scim)
<!-- END: TFEnterprise:only name:next-steps -->
