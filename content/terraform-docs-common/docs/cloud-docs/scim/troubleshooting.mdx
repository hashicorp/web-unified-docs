---
page_title: Troubleshoot SCIM provisioning
description: >-
  Diagnose and resolve common issues with SCIM provisioning, including authentication failures, validation errors, team linking
  conflicts, and rate limits.
# START AUTO GENERATED METADATA, DO NOT EDIT
created_at: 2026-08-05T15:05:42-07:00
last_modified: 2026-09-02T17:23:05.000Z
# END AUTO GENERATED METADATA
---

# Troubleshoot SCIM provisioning

This topic describes troubleshooting steps for diagnosing and resolving issues with SCIM provisioning in <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name -->.

Use this page for <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise-side<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform-side<!-- END: TFC:only name:product-name --> behavior, SCIM responses,<!-- BEGIN: TFEnterprise:only name:page-scope --> team linking,<!-- END: TFEnterprise:only name:page-scope --> and operational follow-up. 

If a failure occurs before <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name --> receives the request, check your identity provider provisioning logs first.

For provider-specific setup, assignment, scope, and attribute-mapping checks, use the [configure guides](/terraform/cloud-docs/scim/configure) after these Terraform Enterprise-side checks.

<!-- BEGIN: TFEnterprise:only name:escalation-path -->
## Escalation path

Complete the following steps in order to investigate issues:

1. Check your IdP provisioning logs first for connection, assignment, scope, and attribute-mapping failures.
1. Check Terraform Enterprise audit logs when requests are reaching Terraform Enterprise but the resulting state is unexpected.
1. Check Terraform Enterprise service logs and run diagnostics for repeated `500` errors, reconciliation timeouts, or suspected platform issues.
1. Generate a support bundle and contact HashiCorp support if the issue persists after these checks.
<!-- END: TFEnterprise:only name:escalation-path -->

## Emergency access
<!-- BEGIN: TFEnterprise:only name:emergency-access -->
Terraform Enterprise can grant site administrator access through SCIM. To ensure you can always log in and troubleshoot issues even if SCIM provisioning is misconfigured or your identity provider (IdP) is unavailable, maintain at least one manually managed non-SSO admin account.

### Create a non-SSO admin account

We strongly recommend creating and maintaining a non-SSO admin account before enabling SCIM. This account provides a recovery path if you encounter issues with your SCIM or SAML configuration.

Before troubleshooting SCIM issues, verify that you have a non-SSO admin account for emergency access. This account should use an email address not associated with your identity provider.

To create a non-SSO recovery admin account in the Terraform Enterprise UI, open `https://<TFE HOSTNAME>/public/signup/account`, create the account, and grant it site administrator access.

If you need the Rails console recovery procedure, refer to [Create a non-SSO admin account for recovery](/terraform/enterprise/saml/troubleshooting#create-a-non-sso-admin-account-for-recovery) in the SAML troubleshooting documentation.
<!-- END: TFEnterprise:only name:emergency-access -->

<!-- BEGIN: TFC:only name:emergency-access -->
The identity provider (IdP) doesn't manage members of the owners team when SCIM is enabled. This to preserve emergency access even when SCIM is misconfigured or your IdP is unavailable. Maintain at least two user-owned accounts in the owners team for this purpose.

### Maintain emergency access accounts

We strongly recommend maintaining at least two user-owned accounts, also called **break-glass accounts**, in the owners team before enabling SCIM. These accounts provide a recovery path if you encounter issues with your SCIM or SAML configuration.

Before troubleshooting SCIM issues, verify that you have a user-owned account in the owners team. Use an email address for this account that is not associated with an organization-owned user.

Organization-owned users cannot serve as break-glass accounts because they lack password authentication and require SSO to log in.

Refer to [Owners team and break-glass scenarios](/terraform/cloud-docs/scim/manage#owners-team-and-break-glass-scenarios) for more information.
<!-- END: TFC:only name:emergency-access -->

## Pause SCIM for debugging

<!-- BEGIN: TFEnterprise:only name:pause-intro -->
You can pause SCIM synchronization for the entire site or for individual teams so that you can isolate issues without deleting your configuration.
<!-- END: TFEnterprise:only name:pause-intro -->

<!-- BEGIN: TFC:only name:pause-intro -->
You can disable SCIM synchronization for your organization so that you can isolate issues without deleting your configuration.
<!-- END: TFC:only name:pause-intro -->

<!-- BEGIN: TFEnterprise:only name:pause-site -->
### Pause SCIM for the site

Pausing SCIM at the site level stops all SCIM provisioning requests for the Terraform Enterprise instance.

Terraform Enterprise site admin steps:

1. Navigate to **Site Admin** > **SCIM**.
1. Open **Manage**.
1. Click **Pause SCIM provisioning**.
1. Confirm the action.

For the underlying site-level pause behavior, refer to [Manage SCIM provisioning](/terraform/enterprise/scim/manage#pause-and-resume-scim-provisioning).

While SCIM is paused at the site level:

- Provisioning requests from your IdP to `/scim/v2/Users` and `/scim/v2/Groups` return `403 Forbidden` because SCIM is disabled or paused.
- Existing users, groups, and team links are preserved.
- SCIM tokens remain valid and are not deleted.
- Users can continue to log in using SAML.
- Teams retain their configured links and pause state, and team-level SCIM metadata remains visible.

To resume SCIM synchronization, open **Manage** and click **Resume SCIM provisioning**.

Resuming site-level SCIM does not replay or backfill changes that your identity provider did not send while SCIM was paused. Terraform Enterprise resumes processing only new SCIM requests after you resume SCIM provisioning.
<!-- END: TFEnterprise:only name:pause-site -->

<!-- BEGIN: TFC:only name:pause-site -->
Disabling SCIM at the organization level stops all SCIM provisioning requests for your HCP Terraform organization:

1. Navigate to **Organization settings** > **SCIM provisioning**.
1. Click **Manage**.
1. Click **Disable SCIM**.
1. Confirm the action.

For more information about disabling SCIM, refer to [Manage SCIM provisioning](/terraform/cloud-docs/scim/manage#disable-scim-provisioning).

While SCIM is disabled:

- Changes to users and groups in your IdP are not synchronized to HCP Terraform.
- Existing provisioned users and teams do not change.
- Users can continue to log in using SSO, including SCIM-managed users.

To re-enable SCIM provisioning, navigate to **Organization settings** > **SCIM provisioning** and click **Enable SCIM**.

Re-enabling SCIM does not automatically replay changes that your identity provider made while SCIM was disabled. Restart provisioning in Entra ID, or remove and re-add assignments in Okta, to synchronize any missed changes.
<!-- END: TFC:only name:pause-site -->

<!-- BEGIN: TFEnterprise:only name:pause-team -->
### Pause SCIM for an individual team

You can pause SCIM synchronization for a specific team without affecting other linked teams.

Terraform Enterprise site admin steps in the UI:

1. Navigate to the team's **Team Settings** tab.
1. In the **SCIM Sync** section, click **Pause Sync**.
1. Confirm the action.

You can also pause or resume SCIM synchronization with the [Team SCIM Group Mapping API](/terraform/enterprise/api-docs/admin/team-scim-group-mapping).

For the full procedure, refer to [Link SCIM groups to teams](/terraform/enterprise/scim/manage/team-mapping#pause-synchronization).

While team-level synchronization is paused:

- The team retains its current membership.
- SCIM updates to the linked group do not affect the team.
- The team remains linked to the SCIM group.
- Other teams linked to the same SCIM group continue to receive updates.
- Direct membership changes in Terraform Enterprise remain blocked while the link exists. To manage membership manually, unlink the team instead. Refer to [Link SCIM groups to teams](/terraform/enterprise/scim/manage/team-mapping).

When you resume synchronization, Terraform Enterprise immediately reconciles the team's membership with the current SCIM group state.
<!-- END: TFEnterprise:only name:pause-team -->

## Check HTTP status codes

You can troubleshoot issues by cross-referencing error code information with your SCIM implementation. For status codes, validation rules, and request details for each endpoint family, refer to the corresponding API reference:

<!-- BEGIN: TFC:only name:endpoint-api-refs -->
- [SCIM API](/terraform/cloud-docs/api-docs/scim)
- [SCIM Users API](/terraform/cloud-docs/api-docs/scim/users)
- [SCIM Groups API](/terraform/cloud-docs/api-docs/scim/groups)
<!-- END: TFC:only name:endpoint-api-refs -->

<!-- BEGIN: TFEnterprise:only name:endpoint-api-refs -->
| Endpoint family | Endpoints | Authentication | API reference |
|-----------------|-----------|----------------|---------------|
| Public SCIM provisioning | `/scim/v2/Users`, `/scim/v2/Groups` | SCIM token | [Public SCIM API](/terraform/cloud-docs/api-docs/scim), [SCIM Users API](/terraform/cloud-docs/api-docs/scim/users), [SCIM Groups API](/terraform/cloud-docs/api-docs/scim/groups) |
| Admin SCIM settings and tokens | `/api/v2/admin/scim-settings`, `/api/v2/admin/scim-tokens` | Site admin API credentials | [Admin SCIM Settings API](/terraform/cloud-docs/api-docs/admin/scim-settings), [Admin SCIM Tokens API](/terraform/cloud-docs/api-docs/admin/scim-tokens) |
| Admin team linking | `/api/v2/admin/teams/:external_id/scim-group-mapping`, `/api/v2/admin/scim-groups` | Site admin API credentials | [Team SCIM Group Mapping API](/terraform/cloud-docs/api-docs/admin/team-scim-group-mapping), [Admin SCIM Groups API](/terraform/cloud-docs/api-docs/admin/scim-groups) |
<!-- END: TFEnterprise:only name:endpoint-api-refs -->

The following table provides an overview of error codes and links to more details and resolution steps.

<!-- BEGIN: TFEnterprise:only name:error-code-table -->
| Code | Meaning | Endpoint family | Resolution |
|------|---------|-----------------|------------|
| `400` | Invalid request format | Public SCIM | [`400 Bad Request` error](#400-bad-request-error) |
| `401` | Authentication failure | Public SCIM | [`401 Unauthorized` errors](#401-unauthorized-errors) |
| `403` | SCIM disabled or paused | Public SCIM | [`403 Forbidden` error](#403-forbidden-error) |
| `409` | Duplicate `userName` or `displayName` | Public SCIM | [Users not syncing](#users-not-syncing), [Groups not syncing](#groups-not-syncing) |
| `413` | Request or group too large | Public SCIM, admin team linking | [`413 Payload Too Large` errors](#413-payload-too-large-errors) |
| `429` | Rate limit exceeded | Public SCIM, admin endpoints | [`429 Too Many Requests` errors](#429-too-many-requests-errors) |
| `500` | Internal server error | Public SCIM, admin endpoints | [`500 Internal Server Error` errors](#500-internal-server-error-errors) |
<!-- END: TFEnterprise:only name:error-code-table -->

<!-- BEGIN: TFC:only name:error-code-table -->
| Code | Meaning | Resolution |
|------|---------|------------|
| `400` | Invalid request format | [`400 Bad Request` error](#400-bad-request-error) |
| `401` | Authentication failure | [`401 Unauthorized` errors](#401-unauthorized-errors) |
| `403` | SCIM disabled | [`403 Forbidden` error](#403-forbidden-error) |
| `409` | Duplicate `userName` or `displayName` | [Users not syncing](#users-not-syncing), [Groups not syncing](#groups-not-syncing) |
| `413` | Request or group too large | [`413 Payload Too Large` errors](#413-payload-too-large-errors) |
| `429` | Rate limit exceeded | [`429 Too Many Requests` errors](#429-too-many-requests-errors) |
| `500` | Internal server error | [`500 Internal Server Error` errors](#500-internal-server-error-errors) |
<!-- END: TFC:only name:error-code-table -->

### `401 Unauthorized` errors

HTTP `401` errors indicate authentication problems with your public SCIM token.

Check for the following conditions:

- The `Authorization` header is missing.
- The `Authorization` header is malformed.
- The token is invalid or expired.
- The token belongs to another token type and is not a SCIM token.

<!-- BEGIN: TFC:only name:token-expiry-note -->
SCIM tokens have a mandatory expiration date between 30 days and one year from creation. Check the token expiration date in **Organization settings** > **SCIM provisioning** to confirm the token has not expired.
<!-- END: TFC:only name:token-expiry-note -->

The <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise site admin<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform organization owner<!-- END: TFC:only name:product-name --> and IdP administrator each have steps to complete to resolve this issue.

<!-- BEGIN: TFEnterprise:only name:401-role-label -->
Terraform Enterprise site admin:
<!-- END: TFEnterprise:only name:401-role-label -->
<!-- BEGIN: TFC:only name:401-role-label -->
HCP Terraform organization owner:
<!-- END: TFC:only name:401-role-label -->

<!-- BEGIN: TFEnterprise:only name:401-admin-steps -->
1. Navigate to **Site Admin** > **SCIM** > **Tokens**.
1. Verify that an active token exists and has not expired.
1. Check the `last-used-at` timestamp to confirm the IdP is successfully authenticating.
1. If necessary, create a new token. SCIM token values are displayed only once when created. If you lose the token value, create a new token and rotate the IdP to use it.
<!-- END: TFEnterprise:only name:401-admin-steps -->

<!-- BEGIN: TFC:only name:401-admin-steps -->
1. Navigate to **Organization settings** > **SCIM provisioning** > **Tokens**.
1. Verify that an active token exists and has not expired.
1. If necessary, generate a new token. Set an expiration date between 30 days and 1 year. SCIM token values are displayed only once when generated. If you lose the token value, generate a new token and update the IdP to use it.
<!-- END: TFC:only name:401-admin-steps -->

IdP administrator:

After the <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise site admin<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform organization owner<!-- END: TFC:only name:product-name --> provides a new token, update the IdP configuration to use the new token and rerun the provisioning test.

### `403 Forbidden` error

HTTP `403` errors returned by the SCIM provisioning endpoints, `/scim/v2/Users` or `/scim/v2/Groups`, usually mean <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name --> is not currently accepting SCIM provisioning requests.

<!-- BEGIN: TFEnterprise:only name:403-paused -->
Check the following:

- SCIM is disabled.
- SCIM is paused at the site level.
- The deployment is not Terraform Enterprise.
<!-- END: TFEnterprise:only name:403-paused -->

<!-- BEGIN: TFC:only name:403-paused -->
Check if SCIM is disabled:

1. Navigate to **Organization settings** > **SCIM provisioning**.
1. Verify that SCIM is enabled.

<!-- END: TFC:only name:403-paused -->

<!-- BEGIN: TFEnterprise:only name:403-paused -->
Terraform Enterprise site admin and IdP administrators must complete tasks to resolve this issue.

Terraform Enterprise site admin:

1. Navigate to **Site Admin** > **SCIM**.
1. Verify that SCIM is enabled.
1. Verify that SCIM provisioning is enabled and not paused.
<!-- END: TFEnterprise:only name:403-paused -->

After checking the SCIM integration status, the IdP administrator should retry provisioning from your IdP after <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name --> is accepting SCIM requests again.

### Users not syncing

If <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name --> does not provision or update users, verify that the request is reaching <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name -->. If not, check your IdP provisioning logs, then check the [configuration guides](/terraform/cloud-docs/scim/configure) for provider-specific assignment, scope, and mapping steps.

<!-- BEGIN: TFEnterprise:only name:users-syncing-conditions -->
Check for the following conditions:

- The user is assigned or otherwise in scope for provisioning in your identity provider.
- The IdP is not sending a `userName` that already exists for another SCIM identity. That condition returns `409 Conflict`.
- The IdP request includes at least one email entry. A primary email is preferred, but Terraform Enterprise can also use the first email entry if none is marked primary. Missing email data returns `400 Bad Request`.
- If Terraform Enterprise already has a manually created user with the same email address, verify that the IdP is sending the same email address for that user so linking can occur.
<!-- END: TFEnterprise:only name:users-syncing-conditions -->

<!-- BEGIN: TFC:only name:users-syncing-conditions -->
Verify that the user is assigned or otherwise in scope for provisioning in your identity provider.
<!-- END: TFC:only name:users-syncing-conditions -->
<!-- BEGIN: TFEnterprise:only name:duplicate-username -->
If the issue is a duplicate `userName`:

1. The Terraform Enterprise site admin identifies the existing SCIM-managed user that already owns that `userName`.
1. The IdP administrator renames the user in the IdP or corrects the conflicting SCIM identity before retrying.
1. Retry provisioning.
<!-- END: TFEnterprise:only name:duplicate-username -->

<!-- BEGIN: TFC:only name:duplicate-user -->
If SCIM creates a new account instead of linking to an existing one, the primary email address sent by SCIM does not match the email address of the existing HCP Terraform account. To resolve:

1. Verify that the primary email address in your IdP matches the email address of the existing HCP Terraform account exactly.
1. Remove the duplicate account and re-sync.

If the organization has two accounts for the same person where one is SCIM-managed and one is manually managed, the SAML `nameID` and the SCIM `userName` are using different IdP attributes. When the stored `scim_username` does not match the `nameID` sent at login, HCP Terraform cannot find the SCIM-managed user and triggers the SSO account-linking flow, which creates a second manually managed account. To resolve:

1. Confirm that the SAML `nameID` and the SCIM `userName` mapping use the same IdP attribute. For Entra ID, both the **Unique User Identifier** in the SAML configuration and the `userName` attribute mapping should use `userPrincipalName`.
1. Remove the manually managed account created through the SSO link flow.
1. Re-provision the user through SCIM.
<!-- END: TFC:only name:duplicate-user -->

<!-- BEGIN: TFEnterprise:only name:missing-scim-user -->
If the issue is a delete request for a missing SCIM user, verify that the IdP is using the current Terraform Enterprise SCIM user `id`. Terraform Enterprise returns HTTP `404 Not Found` for `DELETE /scim/v2/Users/:id` when that SCIM user no longer exists.
<!-- END: TFEnterprise:only name:missing-scim-user -->

### User deactivation or deletion does not behave as expected

<!-- BEGIN: TFEnterprise:only name:deactivation-behavior -->
Terraform Enterprise differentiates between SCIM deactivation and deletion:

- When your IdP sends the `active=false` attribute, Terraform Enterprise suspends the user.
- When your IdP makes a `DELETE` request to the `/scim/v2/Users/:id` endpoint, Terraform Enterprise removes the SCIM identity and suspends the user.
- If the affected user is a SCIM-provisioned site administrator, Terraform Enterprise revokes site admin access.

If the user remains active in Terraform Enterprise, confirm that your IdP is sending either `active=false` or a SCIM delete request, not only removing the user from a group.
<!-- END: TFEnterprise:only name:deactivation-behavior -->

<!-- BEGIN: TFC:only name:deactivation-behavior -->
HCP Terraform differentiates between SCIM deactivation and deletion. The exact behavior depends on your IdP:

- When your IdP deactivates a user, HCP Terraform sets the user status to inactive and removes their ability to log in. Okta typically sends an inactive update immediately. Entra ID initially sets the user to inactive, then may send a delete request after 30 days.
- When your IdP sends a `DELETE` request, HCP Terraform either removes the user from the organization or deletes the user from the system depending on the type of account. User that own and manually manage their accounts are removed, whereas users provisioned and managed in the IdP are deleted.
- Deactivating or deleting a user does not automatically remove the user from teams in HCP Terraform. Remove the user from all group memberships in your IdP before deactivating or deleting them.

If the user remains active in HCP Terraform, confirm that your IdP is sending a deactivation or delete event, not only removing the user from a group.
<!-- END: TFC:only name:deactivation-behavior -->

Deprovision triggers and timing are IdP-specific. If the IdP doesn't send the change you expect, review your IdP provisioning logs and provider-specific deprovisioning guidance.

### Groups not syncing

If SCIM groups are not being provisioned or membership changes are not reflected, verify that the request is reaching <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name -->. If not, check your IdP provisioning logs, then check the [configuration guides](/terraform/cloud-docs/scim/configure) for provider-specific group-push, scope, and mapping steps.

Check for the following conditions:

- The group is configured for provisioning in your identity provider and is in scope for synchronization to <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name -->.
- Group membership updates reference <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name --> SCIM user IDs for users that were already provisioned through `/scim/v2/Users`.
- The IdP is not trying to create or rename a group to a `displayName` that already exists. That condition returns `409 Conflict`.

If the issue is a duplicate group name, rename or remove the conflicting SCIM group in the identity provider<!-- BEGIN: TFEnterprise:only name:duplicate-group --> before retrying.<!-- END: TFEnterprise:only name:duplicate-group --><!-- BEGIN: TFC:only name:duplicate-group -->, then re-trigger synchronization.

HCP Terraform team names have a maximum length of 90 characters. If an IdP group name exceeds 90 characters, HCP Terraform cannot create the corresponding team. Rename the IdP group to 90 characters or fewer before retrying.
<!-- END: TFC:only name:duplicate-group -->

<!-- BEGIN: TFEnterprise:only name:admin-team-linking -->
### Admin team linking fails

If linking a SCIM group to a team fails, check the following:

These are Terraform Enterprise site admin operations. For the full linking workflow, refer to [Link SCIM groups to teams](/terraform/enterprise/scim/manage/team-mapping).

Check for the following conditions:

- The SCIM group has 1,000 or fewer members. Larger groups return `413 Payload Too Large`.
- The team is not already linked to another SCIM group. Existing links return `409 Conflict`.
- The SCIM group is not configured as the site admin group.
- The target team is not the owners team.

To resolve:

1. Split oversized IdP groups into smaller groups if needed.
1. Unlink any existing SCIM group before linking a different one to the same team.
1. Use a different SCIM group for team linking if the current group is the configured site admin group.
1. Leave the owners team manually managed.

### Admin team sync pause or unpause fails

If pausing or unpausing a team link fails:

- Verify that the team is currently linked to a SCIM group.
- Verify that the request uses the admin endpoint `/api/v2/admin/teams/:external_id/scim-group-mapping`.
- Verify that `scim-sync-paused` is a boolean value.

If the team is not linked, the request fails. If the payload omits `scim-sync-paused` or uses a non-boolean value, the request is rejected.

For exact request and response behavior for link pause and resume, including repeated requests, refer to the [Team SCIM Group Mapping API](/terraform/enterprise/api-docs/admin/team-scim-group-mapping).

Only Terraform Enterprise site administrators can link, unlink, pause, or resume SCIM team links.
<!-- END: TFEnterprise:only name:admin-team-linking -->

### `400 Bad Request` error

<!-- BEGIN: TFEnterprise:only name:product-name -->
Terraform Enterprise returns `400` when the SCIM request format is invalid. An invalid format includes unsupported filters or malformed payloads.
<!-- END: TFEnterprise:only name:product-name -->
<!-- BEGIN: TFC:only name:product-name -->
HCP Terraform returns `400` when the SCIM request format is invalid. An invalid format includes unsupported filters or malformed payloads.
<!-- END: TFC:only name:product-name -->

Common causes include:

- Unsupported SCIM filter expressions.
- Malformed JSON.
- Missing required attributes such as an email entry.
- Too many `PATCH` operations in a single request.

To resolve:

1. Compare the IdP request body with the [SCIM Users API](/terraform/enterprise/api-docs/scim/users) or [SCIM Groups API](/terraform/enterprise/api-docs/scim/groups) requirements. Those references document supported `PATCH` forms, filter rules, and request limits.
1. Validate that the JSON is well formed.
1. Confirm that your IdP is only using supported equality filters. Users support `userName eq` and `externalId eq`. Groups support `displayName eq` and `externalId eq`.

### `413 Payload Too Large` errors

The public SCIM endpoint returns an HTTP `413` error when one of the following events occur:

- The request body exceeds the maximum supported SCIM request size
- A request to create or update a SCIM group exceeds the maximum supported group membership.
- The linked SCIM group has more than 1,000 members.

If you receive `413` during public SCIM user or group provisioning calls, inspect both the IdP request payload size and the affected SCIM group membership count. Public group provisioning requests return `413` when they try to create or update a group with more than 1,000 members. Oversized public SCIM requests also return `413`. If you receive `413` while linking a group to a team, reduce the group size before retrying the mapping.

TODO -- verify scim api docs 

For the exact public SCIM request-size and per-endpoint request limits, refer to the [Public SCIM API](/terraform/enterprise/api-docs/scim) and the [SCIM Groups API](/terraform/enterprise/api-docs/scim/groups).

### `429 Too Many Requests` errors

HTTP `429` errors indicate your IdP or administrator is sending requests faster than <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name --> allows.

<!-- BEGIN: TFEnterprise:only name:429-rate-limits -->
Terraform Enterprise applies the following SCIM-related rate limits:

| Endpoint or operation | Rate limit |
|-----------------------|------------|
| Public SCIM `/scim/v2/Users` and `/scim/v2/Groups` endpoints | 10 requests per second, shared across the public SCIM provisioning endpoints |
| Admin SCIM settings endpoint | 20 requests per second |
| Team linking, unlinking, pause, and resume operations | 10 requests per minute per authenticated site administrator |

For public SCIM `/scim/v2/*`, use the `Retry-After` header to determine how long to wait before retrying.

Admin SCIM `429` responses use the standard admin API error format.

For exact rate-limit values and response details, refer to the [Public SCIM API](/terraform/enterprise/api-docs/scim), the [SCIM Settings API](/terraform/enterprise/api-docs/admin/scim-settings), and the [Team SCIM Group Mapping API](/terraform/enterprise/api-docs/admin/team-scim-group-mapping).
<!-- END: TFEnterprise:only name:429-rate-limits -->

<!-- BEGIN: TFC:only name:429-rate-limits -->
TODO - verify scim api docs

For SCIM `/scim/v2/*`, use the `Retry-After` header to determine how long to wait before retrying. Refer to the [SCIM API](/terraform/cloud-docs/api-docs/scim) for for exact rate-limit values and response details.

If you frequently encounter rate limits, ask your IdP administrator to reduce provisioning frequency and avoid repeated full syncs during troubleshooting.
<!-- END: TFC:only name:429-rate-limits -->

<!-- BEGIN: TFEnterprise:only name:429-admin-remediation -->
If you frequently encounter rate limits:

1. For public SCIM `/scim/v2/*`, ask your IdP administrator to reduce provisioning frequency and avoid repeated full syncs during troubleshooting.
1. For admin SCIM settings or tokens, slow down site-level automation or scripted retries.
1. For admin team linking operations, stagger link changes when working with many teams.
<!-- END: TFEnterprise:only name:429-admin-remediation -->

### `500 Internal Server Error` errors

HTTP `500` errors indicate <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name --> failed while processing the request.

For public SCIM `/scim/v2/*`, retry once after confirming the IdP request is valid. If the error repeats, inspect <!-- BEGIN: TFEnterprise:only name:product-name -->Terraform Enterprise<!-- END: TFEnterprise:only name:product-name --><!-- BEGIN: TFC:only name:product-name -->HCP Terraform<!-- END: TFC:only name:product-name --> logs and diagnostics.

<!-- BEGIN: TFC:only name:500-support -->
If `500` errors continue after a retry, contact HashiCorp support. Refer to [Get support for HCP Terraform](/terraform/cloud-docs/deploy/troubleshoot/contact-support) for instructions on opening a ticket.
<!-- END: TFC:only name:500-support -->

<!-- BEGIN: TFEnterprise:only name:500-transaction-timeout -->
For admin team linking or reconciliation, `500` errors usually indicate an internal failure such as a transaction timing out.

Large linking operations use a 30-second transaction timeout. When a timeout occurs:

- The transaction rolls back completely.
- No partial membership changes are applied.
- The team retains its previous membership state.
- Your IdP or administrator must retry the operation.

If linking repeatedly times out, reduce the size of the linked SCIM group.
<!-- END: TFEnterprise:only name:500-transaction-timeout -->

<!-- BEGIN: TFEnterprise:only name:500-diagnostics -->
If `500` errors continue after a retry, inspect Terraform Enterprise service logs and run diagnostics. Refer to [Logs](/terraform/cloud-docs/deploy/manage/monitor/logs) and [Perform diagnostics](/terraform/cloud-docs/deploy/troubleshoot/perform-diagnostics).
<!-- END: TFEnterprise:only name:500-diagnostics -->

## Check sync status

<!-- BEGIN: TFEnterprise:only name:sync-status-surface -->
Terraform Enterprise tracks synchronization timestamps for SCIM-managed users and linked teams. These sync-status fields are exposed on Terraform Enterprise API responses outside the public `/scim/v2/*` provisioning surface.
<!-- END: TFEnterprise:only name:sync-status-surface -->

<!-- BEGIN: TFC:only name:sync-status-surface -->
HCP Terraform tracks synchronization timestamps for SCIM-managed users and linked teams. These sync-status fields are visible in the HCP Terraform UI and API responses outside the public `/scim/v2/*` provisioning surface.
<!-- END: TFC:only name:sync-status-surface -->

### User sync status

For SCIM-managed users, the `scim-updated-at` attribute indicates when the user was last synchronized. You can inspect this in `GET /api/v2/account/details` for the current user<!-- BEGIN: TFEnterprise:only name:user-sync-ui --> and in `GET /api/v2/admin/users` responses<!-- END: TFEnterprise:only name:user-sync-ui --> when SCIM is enabled.

<!-- BEGIN: TFC:only name:user-sync-ui -->
In the HCP Terraform UI, navigate to **Organization settings** > **Users** to view the **Last SCIM Sync (UTC)** timestamp and the **SCIM provisioning** status for each user.
<!-- END: TFC:only name:user-sync-ui -->

Use this timestamp to:

- Verify that IdP changes are being applied.
- Identify users that have not been updated recently.
- Troubleshoot synchronization delays.

### Team sync status

For teams linked to SCIM groups, the `scim-updated-at` attribute indicates when the team's membership was last synchronized from the SCIM group. You can inspect this in `GET /api/v2/teams/:external_id` responses.

Use this timestamp to:

- Confirm that membership changes are propagating.
- Identify teams that may have synchronization issues.
- Verify that unpausing synchronization applied updates correctly.

<!-- BEGIN: TFEnterprise:only name:team-sync-fields -->
Also check `scim-linked`, `scim-group-name`, and `scim-sync-paused` to confirm the link state.
<!-- END: TFEnterprise:only name:team-sync-fields -->

<!-- BEGIN: TFC:only name:team-sync-statuses -->
The following table describes the team statuses that indicate the SCIM provisioning state:

| Status | Description |
|--------|-------------|
| `Synced` | SCIM is enabled, and the team is in sync with your IdP. |
| Not applicable | SCIM has not been enabled, or this team was manually created and is not managed by SCIM. |
<!-- END: TFC:only name:team-sync-statuses -->

<!-- BEGIN: TFEnterprise:only name:site-admin-group -->
## Site admin group issues

If site admin access is not changing as expected, verify that the correct site admin group is configured in **Site Admin** > **SCIM** and that the relevant group membership update or SCIM settings change completed successfully.

For the site admin group behavior, including grant, revoke, remap, and clear semantics, refer to [Manage SCIM provisioning](/terraform/enterprise/scim/manage#site-admin-group-behavior).

Deleting or disabling SCIM does not automatically revoke site admin access that was already granted.
<!-- END: TFEnterprise:only name:site-admin-group -->

<!-- BEGIN: TFEnterprise:only name:audit-logs -->
## Audit logs

For compliance and troubleshooting purposes, Terraform Enterprise logs SCIM operations. SCIM-related audit log entries include:
<!-- END: TFEnterprise:only name:audit-logs -->

<!-- BEGIN: TFC:only name:audit-events -->
## Audit events

For compliance and troubleshooting purposes, HCP Terraform logs SCIM operations. SCIM-related audit events include:
<!-- END: TFC:only name:audit-events -->

- User provisioning, replacement, update, and deprovisioning.
- Group creation, update, membership changes, and deletion.

<!-- BEGIN: TFEnterprise:only name:audit-events -->
- Team-to-group link creation and removal.
- Team sync pause or resume actions.
<!-- END: TFEnterprise:only name:audit-events -->

- SCIM configuration changes.
- Token creation and revocation.

<!-- BEGIN: TFEnterprise:only name:audit-logs -->
Use audit logs to:
<!-- END: TFEnterprise:only name:audit-logs -->

<!-- BEGIN: TFC:only name:audit-events -->
Use audit events to:
<!-- END: TFC:only name:audit-events -->

- Track user access changes for compliance reporting.
- Investigate unexpected permission changes.
- Verify that IdP changes are being applied correctly.
- Debug issues by reviewing the sequence of operations.

<!-- BEGIN: TFEnterprise:only name:audit-log-link -->
To inspect audit logs and service logs, refer to [Logs](/terraform/cloud-docs/deploy/manage/monitor/logs).
<!-- END: TFEnterprise:only name:audit-log-link -->

<!-- BEGIN: TFC:only name:audit-log-link -->
To inspect audit events, refer to the [audit trails API documentation](/terraform/cloud-docs/api-docs/audit-trails).
<!-- END: TFC:only name:audit-log-link -->

<!-- BEGIN: TFEnterprise:only name:disable -->
## Disable SCIM

<Warning>

Disabling SCIM is destructive. If you enable SCIM again later, you must recreate SCIM tokens, reprovision groups, and recreate team mappings. Use it only as a last resort after capturing exact timestamps, relevant IdP log entries, Terraform Enterprise audit or service logs, and a support bundle for further investigation. If you re-enable SCIM, you must restart provisioning in your IdP to synchronize any changes made while SCIM was disabled. Use it only as a last resort after capturing exact timestamps, relevant IdP log entries, and HCP Terraform audit logs for further investigation.

</Warning>

Refer to [Disable SCIM provisioning](/terraform/cloud-docs/scim/manage#disable-scim-provisioning) for the full impact, UI steps, and API behavior.

<!-- END: TFEnterprise:only name:disable -->

<!-- BEGIN: TFC:only name:delete -->
## Delete SCIM configuration

<Warning>

You should only delete the SCIM configuration if other troubleshooting steps have failed. Before deleting, capture timestamps, relevant IdP log entries, and audit events for further investigation. After deleting a SCIM configuration, you must recreate tokens, reprovision groups, and resync users to recreate the configuration. You must restart provisioning in your IdP to synchronize changes after recreating your SCIM configuration. 

</Warning>

Refer to [Delete SCIM provisioning](/terraform/cloud-docs/scim/manage#delete-scim-provisioning) for the full impact, UI steps, and API behavior.

<!-- END: TFC:only name:delete -->

## Contact support

If you cannot resolve your SCIM issue using the troubleshooting steps in this topic, contact HashiCorp support. <!-- BEGIN: TFEnterprise:only name:get-support -->Refer to [Get support for Terraform Enterprise](/terraform/enterprise/deploy/troubleshoot/contact-support) for instructions on opening a ticket, creating diagnostic support bundles, and other information.<!-- END: TFEnterprise:only name:get-support --><!-- BEGIN: TFC:only name:get-support -->Visit the [IBM support page](https://www.ibm.com/mysupport/s/?language=en_US) for support articles and to open a ticket. <!-- END: TFC:only name:get-support -->
