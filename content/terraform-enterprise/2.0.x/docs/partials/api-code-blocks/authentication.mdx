All system API endpoints require authentication with a bearer token generated using the `tfectl admin api-token generate` command. For more information on token creation and management, refer to the [tfectl documentation](/terraform/enterprise/deploy/reference/cli).

Use the HTTP Header `Authorization` with the value `Bearer <token>`.
