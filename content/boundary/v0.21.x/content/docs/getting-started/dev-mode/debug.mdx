---
layout: docs
page_title: Enable debug mode
description: >-
  Boundary includes a small number of runtime tracing user regions, which can show where Boundary spends its time during execution. Exposing the pprof endpoint requires enabling the runtime `-debug` flag on a process with an `ops` listener.
# START AUTO GENERATED METADATA, DO NOT EDIT
created_at: 2026-04-27T17:12:43-06:00
last_modified: 2026-04-28T20:22:45.000Z
# END AUTO GENERATED METADATA
---

# Enable debug mode

The `debug` flag enables debug mode. Debug mode exposes pprof endpoints on the `ops` listener.

Gathering information about the state of the Boundary cluster often requires the
operator to access all necessary information using API calls and terminal
commands. The `debug` flag offers a simple workflow that produces consistent output to help operators retrieve and share information about the controller or worker server in question.

Boundary includes a small number of runtime tracing user regions, which you can use to see where Boundary spends its time during execution. To create a trace you must expose the pprof endpoint, which is disabled by default. To expose the pprof endpoint you must enable the runtime `-debug` flag on a process with an `ops` listener.

Debug mode exposes the following pprof endpoints:

- /debug/pprof/
- /debug/pprof/heap
- /debug/pprof/profile
- /debug/pprof/block
- /debug/pprof/mutex
- /debug/pprof/goroutine
- /debug/pprof/threadcreate
- /debug/pprof/trace
- /debug/pprof/cmdline
- /debug/pprof/symbol

The `debug` flag honors the same variables that the base `dev` and `server` commands accept. You can use the debug flag with the [server](/boundary/docs/commands/server) command to run a trace for controller and worker servers.

For more information about how to use the endpoints, refer to the [pprof](https://pkg.go.dev/net/http/pprof) Go documentation or the [Profiling Go programs](https://go.dev/blog/pprof) blog post.

## Example

Here's an example of enabling debug using `boundary dev`:

```shell-session
$ boundary dev -debug -ops-listen-address=127.0.0.1:9203
```

This will expose the pprof endpoints on the configured ops listener. With the example above that means `127.0.0.1:9203`, and only accessible to users on the same machine. This is obvious for dev mode, but also applies to the `server` command on controllers or workers.

Here's an example of enabling debug using `boundary server`:

```shell-session
$ boundary server -config=/etc/boundary.d/controller.hcl -debug -ops-listen-address=127.0.0.1:9203
```

To create a trace, one can use any tool that allows creating HTTP requests, e.g. `curl`. To create a 3 second trace:

```shell-session
$ curl -o trace.out http://127.0.0.1:9203/debug/pprof/trace?seconds=3
```

Traces are most interesting if they contain some request handling. We recommended preparing some HTTP requests that trigger the behavior you want to understand while the trace is being collected.