The Credential Manager manages credential stores, libraries, and individual credentials that let users authenticate to targets.
They are responsible for secure credential management and access.
Credential Managers are frequently members of security teams.
Use cases include:
- Rotating credentials
- Managing secrets
- Integrating Boundary with secret stores

Recommended permissions for the Org and/or Project scope:
```shell-session
type=credential-store;actions=create,list
ids=*;type=credential-store;actions=read,update,delete
ids=*;type=credential-library;actions=create,read,update,delete
ids=*;type=credential;actions=create,read,update,delete
```