The Org Admin manages a specific organization, with full control over authentication methods, user accounts, group memberships, and role assignments in that organization.
It serves as the highest administrative role at the organization level.

Use cases could include:
- Managing projects within an org
- Setting up auth methods
- Managing users at an org level
- Configuring storage buckets

We recommend the following permissions for Org Admins, assigned to the org scope the administrator manages.

Permit any actions on any resource types in the specified org scope:
```shell-session
ids=<org-id>;type=scope;actions=*
```
Permit any actions on auth methods:
```shell-session
ids=*;type=auth-method;actions=*
```
Permit any actions on users:
```shell-session
ids=*;type=user;actions=*
```
Permit any actions on groups:
```shell-session
ids=*;type=group;actions=*
```
Permit any actions on roles:
```shell-session
ids=*;type=role;actions=*
```