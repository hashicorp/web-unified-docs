The Target Manager creates and manages targets, which are services that users can connect to.
Their responsibilities can include configuring which hosts, credentials, and resources are available to users.
Target Managers are frequently DevOps engineers or system administrators.

Uses cases could include:
- Adding new services to Boundary
- Updating target configurations

We recommend the following permissions for Target Managers, assigned at the org or project scope level.

<table style={{width:'100%', tableLayout:'fixed'}}>
  <thead>
    <tr>
      <th style={{verticalAlign:'middle', width:'30%'}}>Description</th>
      <th style={{verticalAlign: 'middle'}}>Grants</th>
    </tr>
  </thead>
  <tbody>

  <tr>
    <td style={{verticalAlign: 'middle'}}>
    Create and list targets.
    </td>

    type=target;actions=create,list

  </tr>

  <tr>
    <td style={{verticalAlign: 'middle'}}>
    Read, update, and delete targets.
    </td>

    ids=*;type=target;actions=read,update,delete

  </tr>

  <tr>
    <td style={{verticalAlign: 'middle'}}>
    Add host sources to a target, define the full set of host sources on a target, and remove host sources from a target.
    </td>

    ids=*;type=target;actions=add-host-sources,set-host-sources,remove-host-sources

  </tr>

  <tr>
    <td style={{verticalAlign: 'middle'}}>
    Add credential sources to a target, define the full set of credential sources on a target, and remove credential sources from a target.
    </td>

    ids=*;type=target;actions=add-credential-sources,set-credential-sources,remove-credential-sources

  </tr>

  </tbody>
</table>