- `-id=<string>` - The ID of the org or project scope for which you want to create the suffix.
You can also specify the scope using the **BOUNDARY_SCOPE_ID** environment variable.
- `-alias-suffix` - Specifies the string that you want to use as the suffix to append to aliases in the designated scope or scopes.
The `alias-suffix` can be a hostname or a DNS-like string.