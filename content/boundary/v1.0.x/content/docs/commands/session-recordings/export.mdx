---
layout: docs
page_title: session-recordings export - Command
description: >-
  The "session-recordings export" command exports RDP session recording resources as video files so that you can view them.
# START AUTO GENERATED METADATA, DO NOT EDIT
created_at: 2025-11-18T17:04:41-05:00
last_modified: 2026-01-27T10:52:39-05:00
# END AUTO GENERATED METADATA
---

# session-recordings export

Command: `boundary session-recordings export`

The `boundary session-recordings export` command lets you export RDP session recording resources as video files so that you can view them.

## Example

The following command exports a session recording with the ID `cr_1234567890` using the webm mime-type:

```shell-session
$ boundary session-recordings export -connection-recording-id cr_1234567890 -mime-type video/webm
```

## Usage

<CodeBlockConfig hideClipboard>

```shell-session
$ boundary session-recordings export [options] [args]
```

</CodeBlockConfig>

### Command options

- `-connection-recording-id=<string>` - The ID of the connection recording that you want to export.
- `-mime-type=<string>` - The export's mime type. It must be a valid Boundary API mime type.

### Subcommands

The `session-recordings export` command has two subcommands, `cancel` and `read`:

<Tabs>
<Tab heading="Cancel">

The `cancel` command cancels a session recording export.

#### Example

The following example cancels a session recording export with the ID `exp_1234567890`:

```shell-session
$ boundary session-recordings export cancel -id exp_1234567890
```

#### Usage

<CodeBlockConfig hideClipboard>

```shell-session
$ boundary session-recordings export cancel [options] [args]
```

</CodeBlockConfig>

#### Cancel options

The following are specific options in addition to the command options:

- `-id=<string>`  - The ID of the session recording export that you want to cancel.

</Tab>
<Tab heading="Read">

The `read` command reads a session recording export.

#### Example

The following example reads a session recording export with the ID `exp_1234567890`:

```shell-session
$ boundary session-recordings export read -id exp_1234567890
```

#### Usage

<CodeBlockConfig hideClipboard>

```shell-session
$ boundary session-recordings export read [options] [args]
```

</CodeBlockConfig>

#### Read options

The following are specific options in addition to the command options:

- `-id=<string>`  - The ID of the session recording export that you want to read.

</Tab>
</Tabs>

@include 'cmd-option-note.mdx'
