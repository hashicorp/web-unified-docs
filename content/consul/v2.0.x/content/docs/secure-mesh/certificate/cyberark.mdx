---
layout: docs
page_title: Use CyberArk Workload Identity Manager as a service mesh certificate authority
description: >-
  You can use CyberArk Workload Identity Manager as the Consul service mesh's certificate authority to secure your service mesh. Learn how to configure Consul to use CyberArk Workload Identity Manager as the service mesh's certificate authority.
# START AUTO GENERATED METADATA, DO NOT EDIT
created_at: 2026-04-22T14:47:26.000Z
last_modified: 2026-04-22T14:47:26.000Z
# END AUTO GENERATED METADATA
---

# Use CyberArk Workload Identity Manager as a service mesh certificate authority

This page describes the use of CyberArk Workload Identity Manager as the certificate authority for the Consul service mesh.

## Introduction

You can configure Consul to use CyberArk Workload Identity Manager as the certificate authority (CA) so that Workload Identity Manager can manage and sign certificates distributed to services in the mesh.

## Requirements

- Consul 2.0.0 or higher

## Recommendations

- Refer to [Service Mesh Certificate Authority Overview](/consul/docs/secure-mesh/certificate) for important background information about how Consul manages certificates with configurable CA providers.


## Enable CyberArk Workload Identity Manager as the CA

You can enable CyberArk Workload Identity Manager as the CA by configuring Consul to use `"cyberark-wim"` as the CA provider
and including the required provider configuration options.
You can provide the CA configuration in the server agents' configuration file or in the body of a `PUT` request to the
[`/connect/ca/configuration`](/consul/api-docs/connect/ca#update-ca-configuration) API endpoint.
Refer to the [Configuration Reference](#configuration-reference) for details about configuration options and for example use cases.

The following example shows the required configurations for a default implementation:

<CodeTabs heading="Service mesh CA configuration" tabs={["Agent configuration", "API"]}>

<CodeBlockConfig filename="/etc/consul.d/config.hcl">

```hcl
## ...
connect { 

  enabled     = true 
  ca_provider = "cyberark-wim" 

  ca_config { 
    # Auth options  
    token = "..."  

    address = "https://localhost:8002" 
    chainAddress = "http://localhost:8008" 
    policy = "Demo Policy" 

    leaf_cert_ttl         = "72h" 
    intermediate_cert_ttl = "288h" 
    root_cert_ttl         = "8760h" 
  } 
}
## ... 
```

</CodeBlockConfig>

<CodeBlockConfig>

```json

```

</CodeBlockConfig>

</CodeTabs>

## Configuration Reference

You can specify the following configuration options.
Note that a configuration option's name may differ between API calls and the agent configuration file.
The first key refers to the option name for use in API calls.
The key after the slash refers to the corresponding option name in the agent configuration file.

- `Address` / `address` (`string: <required>`) - The address of the CyberArk Workload Identity Manager server.

- `Token` / `token` (`string: ""`) - A token for accessing CyberArk Workload Identity Manager.

- `ChainAddress` / `chainAddress` (`string: ""`) - 

- `Policy` / `policy` (`string: ""`) - 

@include 'legacy/http_api_connect_ca_common_options.mdx'


