---
layout: docs
page_title: Multiport feature support
description: >-
  Review the support status of Consul service mesh features when used with multiport services.
---

# Multiport feature support

@include 'alerts/enterprise-alert.mdx'

This page summarizes the support status of Consul service mesh features when used with [multiport services](/consul/docs/connect/proxy/multiport).

<Note>

This feature is under active development and validation phase, and only supported in Consul Enterprise.

</Note>

## Feature support matrix

| Feature | Status | Description |
| --- | --- | --- |
| Cross-partition | Partial | Validated for Kubernetes with Consul admin partitions; other platforms aren't separately validated. |
| Cluster peering | No | Not currently supported. |
| Upstream: transparent proxy mode | Yes | — |
| Upstream: non-transparent proxy mode | Yes | — |
| Multi-protocol (L7) | Partial | Only the default port fully traverses a configured `service-router`, `service-splitter`, or `service-resolver`. Named, non-default ports connect directly to the destination port instead of traversing the chain. |
| API gateway | No | Not currently supported. |
| Terminating gateway | No | Not currently supported. |
| Ingress gateway | No | [Ingress gateway](/consul/docs/north-south/ingress-gateway) is deprecated. |
| Discovery chain: splitter, resolver, and router | Partial | Only the default port fully traverses the configured chain. Non-default named ports connect directly to the destination port instead of being routed through it. |
| Proxy and service defaults: destination port | No | Not currently supported. |
| Proxy and service defaults: source port | No | Not currently supported. |
| Escape hatches, fault injection, and other overrides | No | Not currently supported. |
| Port-specific intentions (ACLs) | Partial | Intentions are enforced at the service level. Named-port virtual IPs resolve to the base service for intention evaluation. |
| Per-port TLS permissiveness override | No | Not currently supported. |
| Per-port metrics and tracing | Partial | Each named port gets its own Envoy cluster, enabling per-cluster metrics. Distributed tracing doesn't yet include port-level tags. |
| Port-wise health checks and reconciliation | No | Not currently supported. |
| Platform: Kubernetes | Yes | — |
| Platform: ECS, ESM, and Terraform modules | No | Not currently supported. |
| DNS compatibility | Partial | Named ports resolve via DNS SRV lookups; other DNS record types don't disambiguate between ports. |
| DNS: localized DNS | No | Not currently supported. |
| DNS: prepared queries | No | Not currently supported. |
| Passthrough mode (transparent proxy) | No | Not currently supported. |
| Per-port watches and scale testing | No | Not currently supported. |

## Related pages

- [Multiport services](/consul/docs/connect/proxy/multiport)
- [Configure multi-port services on Kubernetes](/consul/docs/connect/k8s/multiport)
- [Consul service mesh documentation](/consul/docs/connect)
- [Proxy configuration reference](/consul/docs/connect/proxy)
