---
page_title: 'nomad command subcommand command reference'
description: |
  The `nomad command subcommand` command does the thing it does. 130-160
  characters.
---

# `nomad command subcommand` command reference

One sentence describing the operation the command performs.

<EnterpriseAlert />

## Usage

Nomad puts the syntax and the full description inside the usage block, rather
than giving the description its own heading:

```plaintext
nomad command subcommand [options] <argument>
```

The `command subcommand` command `<does the thing>`. Describe the behavior, the
arguments, and what the command does when the target does not exist.

When ACLs are enabled, this command requires the `<capability>` capability for
the `<resource>`.

## Options

- `-option`: `<What it does.>` Give the accepted values and the default.

- `-other-option`: `<What it does.>`

- `-verbose`: Show full information.

## General options

Nomad names this block **General options**, where the global template says
**Global flags**:

@include 'general_options.mdx'

## Examples

Introduce each example with the operation it performs:

`<Introduce the example.>`

```shell-session
$ nomad command subcommand real-argument

<real output, pasted verbatim from a run>
```
