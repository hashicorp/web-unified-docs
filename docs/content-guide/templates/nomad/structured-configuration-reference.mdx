---
page_title: '`block_name` block in the job specification'
description: |
  Configure the `block_name` block to do what it does. 130-160 characters,
  front-loaded with the block name.
---

# `block_name` block in the job specification

The `block_name` block `<does what it does>`. State where the block is valid and
what encloses it.

```hcl
job "example" {
  group "example" {
    block_name {
      parameter = "value"
    }
  }
}
```

## Parameters

Nomad names this block **Parameters**, where the global template uses
**Configuration model**, **Complete configuration**, and **Specification**.

List required parameters first in alphabetical order, then optional parameters in
alphabetical order.

- `parameter_name` `(<type>: <default>)` - `<What it configures.>` `<Accepted
  values.>` `<What happens when it is omitted.>`

- `other_parameter` `(<type>: <default>)` - `<What it configures.>`

- `nested_block` `(<block>: nil)` - `<What it configures.>` Refer to
  [`nested_block`](link) for its parameters.

## Examples

Introduce each example with what it configures, then show a complete, valid
block rather than a fragment:

### Common case

`<Introduce the example.>`

```hcl
block_name {
  parameter = "value"
}
```

### Variation

`<Introduce the variation and say how it differs.>`

```hcl
block_name {
  parameter       = "value"
  other_parameter = 3
}
```
