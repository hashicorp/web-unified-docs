---
page_title: "command subcommand - Command"
description: |-
  The "command subcommand" command does the thing it does.
---

# command subcommand

The `command subcommand` command `<does the thing>`. Two or three sentences
describing the behavior, including what happens when the target does not exist
or holds no data.

## Examples

Introduce each example with the operation it performs, then show the command and
its real output:

```shell-session
$ vault command subcommand -mount=secret real-argument

<real output, pasted verbatim from a run>
```

`<Introduce a variation, such as different plugin version behavior.>`

```shell-session
$ vault command subcommand -mount=secret -field=name real-argument
<real output>
```
