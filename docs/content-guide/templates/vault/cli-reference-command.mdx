---
page_title: "command subcommand - Vault CLI"
description: >-
  Use vault command subcommand to do the thing it does. 130-160 characters,
  front-loaded with the command name.
---

# `command subcommand`

One sentence describing the operation the command performs. This sentence also
serves as the description in the embedded CLI help, so write it to stand alone.

<CodeBlockConfig hideClipboard>

```shell-session
$ vault command subcommand [flags] <argument> [options]

$ vault command subcommand [-help | -h]
```

</CodeBlockConfig>

## Description

Full description of what the command does and when a reader would use it. Keep
it to behavior — link out to usage and concept pages rather than explaining
workflows here.

<Tip title="Related API endpoints">

  OperationName - [`POST:/sys/ENDPOINT`](/vault/api-docs/system/ENDPOINT)

</Tip>

## Command arguments

Compose argument descriptions from partials under
`content/vault/<version>/content/partials/cli/`. Do not write them inline: the
same argument appears on multiple pages, and the partial is what keeps them
consistent.

<br />

@include 'cli/COMMAND/args/ARGUMENT_NAME.mdx'

<br /><hr /><br />

@include 'cli/COMMAND/args/OTHER_ARGUMENT.mdx'

## Command options

<br />

@include 'cli/COMMAND/options/OPTION_NAME.mdx'

<br /><hr /><br />

@include 'cli/COMMAND/options/OTHER_OPTION.mdx'

## Command flags

<br />

@include 'cli/COMMAND/flags/FLAG_NAME.mdx'

<br /><hr /><br />

@include 'cli/shared/flags/SHARED_FLAG.mdx'

## Standard flags

<br />

@include 'cli/standard-settings/all-standard-flags-but-format.mdx'

## Examples

Introduce every example with the operation it performs, then show the command
and its real output:

```shell-session
$ vault command subcommand real-argument -flag=real-value
Success! <real output>
```

`<Introduce the second example.>`

```shell-session
$ vault command subcommand real-argument -other-flag=real-value
Success! <real output>
```
