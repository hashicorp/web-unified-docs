---
page_title: Resource name resource
description: >-
  Learn how the RESOURCE resource does what it does. Understand how it relates
  to the resources around it. 130-160 characters.
---

# Resource names

{/*
  The H1 is the plural resource name. The page_title is the singular resource
  name followed by "resource".
*/}

A `<resource>` is a resource
that represents `<what it represents>`.
Describe what the resource is, then its relationships to other domain model
resources, then where it can be defined.

Link every other domain model resource you name using reference-style links —
`[scope][]`, `[roles][]` — and define them at the end of the Referenced by
section below. Define both singular and plural forms so prose reads naturally.

A `<resource>` can be defined at either a [global][] or [organization][]
[scope][].

## Resource-specific section

Optional, and there may be several. Use H2 sections before Attributes for
behavior a reader needs in order to understand the attributes — registration,
resource subtypes, calculation rules. Name them for the subject.

## Attributes

A `<resource>` has the following configurable attributes:

- `name` - (optional)
  If set, the `name` must be unique within the `<resource>`'s immediate parent
  `<parent resource>`.

- `description` - (optional)

- `attribute_name` - (optional)
  What it configures, and what Boundary does when it is not set. Keep the first
  line to a single sentence and continue in following lines.

  Use a second paragraph for detail such as accepted formats, and an ordered
  list when the resource applies a priority order:

  1. First
  1. Second

### Subtype attributes

Use an H3 when a subtype — a plugin-based variant, a specific credential type —
adds its own attributes.

- `attribute_name` - (optional)
  What it configures.

## Referenced by

List the domain model resources that reference this one. Entries are
reference-style links with no inline destination:

- [Resource name][]
- [Other resource][]

{/*
  Define every reference-style link used anywhere on the page here, at the end
  of the Referenced by section and before Service API docs. Define singular and
  plural forms separately, sorted alphabetically.
*/}

[global]: /boundary/docs/domain-model/scopes#global
[organization]: /boundary/docs/domain-model/scopes#organizations
[other resource]: /boundary/docs/domain-model/other-resources
[resource name]: /boundary/docs/domain-model/resource-names
[scope]: /boundary/docs/domain-model/scopes

## Service API docs

The following services are relevant to this resource:

- [Resource name service](/boundary/api-docs/RESOURCE-service)

{/*
  Boundary's API reference is generated from protocol buffers and published
  outside this repository. Link to the service page; do not describe the
  endpoints here.
*/}

## Tutorial

Optional. Refer to the [tutorial name](link) tutorial to `<do the thing>`.
