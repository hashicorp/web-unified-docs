---
page_title: "command subcommand - Command"
description: >-
  The "command subcommand" command does the thing it does. 130-160 characters.
---

# command subcommand

Command: `boundary command subcommand`

The `command subcommand` command lets you `<do the thing>`. One or two sentences.

## Examples

Boundary places examples before the usage block. Introduce each example with the
operation it performs:

```shell-session
$ boundary command subcommand -id abc_1234567890 \
   -name devops
```

**Example output:**

<CodeBlockConfig hideClipboard>

```plaintext
<real output, pasted verbatim from a run>
```

</CodeBlockConfig>

## Usage

<CodeBlockConfig hideClipboard>

```shell-session
$ boundary command subcommand [options] [args]
```

</CodeBlockConfig>

### Command options

Boundary documents options as an H3 inside the usage block, not as its own H2:

- `-description` `(string: "")` - `<What it sets.>`
- `-id` `(string: "")` - `<What it identifies.>`
- `-name` `(string: "")` - `<What it sets.>`

#### Usages by type

Delete this section when the command takes no type argument. When it does, give
each type its own tab, and repeat the example, usage, and type-specific options
inside the tab:

<Tabs>
<Tab heading="First type">

The `command subcommand first-type` command lets you `<do the thing>`.

#### Example

`<Introduce the example.>`

```shell-session
$ boundary command subcommand first-type \
   -id abc_1234567890
```

#### Usage

<CodeBlockConfig hideClipboard>

```shell-session
$ boundary command subcommand first-type [options] [args]
```

</CodeBlockConfig>

#### First type options

The following options are specific to `<first type>` in addition to the command
options:

- `-option` `(string: "")` - `<What it does.>`

</Tab>
<Tab heading="Second type">

`<Repeat the structure above for each remaining type.>`

</Tab>
</Tabs>

@include 'cmd-option-note.mdx'
