---
page_title: meta_argument meta-argument reference
description: >-
  Use the META_ARGUMENT meta-argument to do what it does. 130-160 characters,
  front-loaded with the meta-argument name.
---

# The `meta_argument` meta-argument

Name the meta-argument and what it changes about the block it appears in. One or
two sentences. Link the concept it depends on rather than explaining it here.

## Usage

Show the meta-argument in a block, then explain its accepted values and how
Terraform evaluates it:

```hcl
resource "example_resource" "example" {
  meta_argument = <value>
}
```

Describe what Terraform does with the value, what it accepts, and what it rejects
— particularly anything that must be known at plan time.

{/*
  Some meta-arguments carry an extra H2 here for behavior a reader needs before
  the constructs table: planning consequences, module-developer guidance. Name it
  for the subject.
*/}

## Supported constructs

State which block types accept this meta-argument. A table scans better than
prose when the answer differs by block type:

| Construct | Supported |
| --- | --- |
| `resource` | `<yes / no>` |
| `data` | `<yes / no>` |
| `module` | `<yes / no>` |
| `provider` | `<yes / no>` |

## Example use cases

One H3 per use case, each with a complete, runnable example:

### Use case name

`<What the reader is trying to achieve.>`

```hcl
resource "example_resource" "example" {
  meta_argument = <value>
}
```
