---
page_title: Terraform Enterprise vYYYYMM-N release
description: >-
  Release notes for Terraform Enterprise vYYYYMM-N. 130-160 characters, naming
  the version and the headline change.
---

# Terraform Enterprise vYYYYMM-N

{/*
  Headings use the fixed set below, in this order. Delete any heading that has no
  content for the release rather than leaving it empty. The set is title case,
  matching the 83 existing release pages; this is deliberate and is the one place
  in the guide where title case is preserved.
*/}

## Known Issues

- `<Describe the issue, its impact, and the workaround if one exists.>`

## Breaking Changes

- `<Describe what breaks, who it affects, and what the reader must do.>`

## Deprecations

- `<Name what is deprecated, when it is removed, and the replacement.>`

## Highlights

- `<One to three entries. The changes worth reading if the reader reads nothing
  else.>`

## Features

- `<Name the feature and what it lets the reader do. Link the documentation.>`

## Improvements

- `<Describe the change in behavior, not the internal work that produced it.>`

## Bug Fixes

- `<Describe the symptom the reader would have seen, then the fix.>`

## Security

- `<Describe the issue at the level of detail the security team has approved,
  with the CVE identifier where one exists.>`
