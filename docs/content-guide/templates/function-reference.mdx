---
page_title: "`function_name` function reference"
description: >-
  The `function_name` function returns what it returns, phrased in active voice
  and front-loaded with the function name.
---

# `function_name` function reference

`function_name` returns `<what it returns>`. One sentence, active voice, starting
with the function name.

```hcl
function_name(first_argument, second_argument)
```

Describe the behavior the signature does not convey: how each argument is
interpreted, what the function does at its boundaries, and what it does with
unusual input. Name the arguments exactly as the signature names them.

State error conditions explicitly. A reader who hit an error is a common visitor
to this page.

If the language has built-in syntax that does the same job, say so and say when
this function is still the right choice.

## Examples

Show the call and its result in the same block, in the format the language
console produces. The common case comes first and usually needs no introduction:

```
> function_name(["a", "b", "c"], 1)
"b"
```

Introduce each remaining example with a single sentence describing the edge case
it covers:

```
> function_name(["a", "b", "c"], 3)
"a"
```

To `<achieve some specific outcome>`, `<do the thing>`:

```
> function_name(["a", "b", "c"], -1)
"c"
```

## Related functions

- [`sibling_function`](link) `<one clause on how it differs>`.
- [`other_function`](link) `<one clause on how it differs>`.
