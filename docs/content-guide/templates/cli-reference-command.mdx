---
page_title: root-command command subcommand
description: The {full command name} command {what it does, phrased in active voice}.
---

# `{full command name}` reference

One-sentence description.

## Usage

```shell-session
$ {full command name} ARGUMENT --required-flag <value> [options]
```

## Description

Complete description.

## Command arguments

- `ARGUMENT-TEXT`: Description.
    - This argument is {required || optional}.
    - Data type.
    - Default or inherited value.
    - You must {use the Enteprise edition || have a Plus account || etc. } to specify this argument.

## Flags

- `flag-name`: Description.
    - {Required to {perform task} || optional}.
    - Data type.
    - Default or inherited value.
- `flag-name`: Description.
    - Data type.
    - Default or inherited value.
    - You must {use the Enteprise edition || have a Plus account || etc. } to specify this option.

## Options

- `--option`: Description.
    - {Required to {perform task} || optional}.
    - Data type.
    - Default or inherited value.
- `--option`: Description.
    - Data type.
    - Default or inherited value.
    - You must {use the Enteprise edition || have a Plus account || etc. } to specify this option.

## Examples

The following command {does X}:

```shell-session
$ full-command REAL-WORLD-ARG --required-flag real-world-value --expected-option real-world-value
```

## Global flags

Refer to [Global flags reference](/{product}/docs/reference/cli/global-flags) for information about flags you can use with all commands.

## Related

- [Topic title](link)
